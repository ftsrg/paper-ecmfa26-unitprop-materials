class RailwayContainer {
    contains Route[] routes
    contains Region[] regions
}
class Region {
    contains Sensor[] sensors
    contains TrackElement elements
}
class Semaphore {
    Signal signal
}
class Route {
    contains SwitchPosition[] follows opposite route
    Sensor[2..*] requires
    Semaphore entry
    Semaphore exit
    boolean active
}
class SwitchPosition {
    container Route route opposite follows
    Switch[1] target opposite positions
    Position position
}
abstract class TrackElement {
    Sensor[] monitoredBy opposite monitors
    TrackElement[] connectsTo
}
class Sensor {
    TrackElement[] monitors opposite monitoredBy
}
class Segment extends TrackElement {
    contains Semaphore[] semaphores
    int length
}
class Switch extends TrackElement {
    SwitchPosition[] positions opposite target
    Position currentPosition
    TrackElement straight
    TrackElement diverging
}
enum Position {
    FAILURE,
    STRAIGHT,
    DIVERGING
}
enum Signal {
    FAILURE,
    STOP,
    GO
}


error sameStraightDiverging(switch, target) <->
    straight(switch,target),
    diverging(switch,target).

error noDiverging(switch) <->
    Switch(switch),
    !diverging(switch,_).

error noStraight(switch) <->
    Switch(switch),
    !straight(switch,_).

error tooManyConncetions(segment) <->
    Segment(segment),
    count{connectsTo(segment, _)} > 2.


error selfLoop(e) <->
    TrackElement(e),
    connectsTo(e, e)
;
    Switch(e),
    straight(e, e)
;
    Switch(e),
    diverging(e, e).


error underconnectedSwitch(sw) <->
    Switch(sw),
    !switchWithTwoConnections(sw, _, _).

pred switchWithTwoConnections(sw, te1, te2) <->
    Switch(sw),
    connectsTo(sw, te1),
    connectsTo(sw, te2),
    te1 != te2.


error sensorWithoutElement(sensor) <->
    Sensor(sensor),
    !monitors(sensor, _).

error sensorMonitorsMultipleSwitches(sensor, sw1, sw2) <->
    Sensor(sensor),
    monitors(sensor, sw1),
    monitors(sensor, sw2),
    Switch(sw1),
    Switch(sw2),
    sw1 != sw2.

error duplicateMonitors(region, sensor1, sensor2, te) <->
    Region(region),
    sensors(region, sensor1),
    sensors(region, sensor2),
    sensor1 != sensor2,
    monitors(sensor1, te),
    monitors(sensor2, te).

error emptyRegion(region) <->
    Region(region),
    !elements(region, _).


error posLength(segment) <->
    Segment(segment),
    length(segment) <= 0.

error switchMonitored(sw) <-> 
    Switch(sw),
    !monitoredBy(sw, _).


error routeSensor(route, sensor, swP, sw) <->
    monitoredBy(sw, sensor),
    target(swP, sw),
    follows(route, swP),
    !requires(route, sensor).

error switchSet(semaphore, route, swP, sw) <->
    active(route),
    entry(route, semaphore),
    follows(route, swP),
    target(swP, sw),

    signal(semaphore, GO),
    position(swP, swpPosition),
    currentPosition(sw, swCurrentPosition),
    swCurrentPosition != swpPosition.



error connectedSegments(sensor, segment1, segment2, segment3, segment4, segment5, segment6) <->
    connectsTo(segment1, segment2),
    connectsTo(segment2, segment3),
    connectsTo(segment3, segment4),
    connectsTo(segment4, segment5),
    connectsTo(segment5, segment6),
    monitoredBy(segment1, sensor),
    monitoredBy(segment2, sensor),
    monitoredBy(segment3, sensor),
    monitoredBy(segment4, sensor),
    monitoredBy(segment5, sensor),
    monitoredBy(segment6, sensor).

error semaphoreNeighbor(route1, route2) <->
    exit(route1, semaphore),
    requires(route1, sensor1),
    monitoredBy(te1, sensor1),
    connectsTo(te1, te2),
    monitoredBy(te2, sensor2),
    requires(route2, sensor2),
    !entry(route2, semaphore),
    route1 != route2.

declare railwayContainer1, sensor1, switch1, route1, region1, switch2,
	switchPosition1, sensor2, region2, sensor3, region3, sensor4, sensor5,
	segment1, sensor6, semaphore1, region4, segment2.
declare region5, segment3, semaphore2, region6, switch3, semaphore3.
declare region7, segment4, semaphore4, semaphore5, semaphore6, semaphore7.
declare region8, switch4, semaphore8, semaphore9, semaphore10, sensor7.
declare region9, region10, sensor8, segment5, switch5, semaphore11,
	semaphore12, semaphore13, semaphore14, semaphore15, sensor9, semaphore16.
declare region11, switch6, region12, segment6.
declare region13, sensor10, semaphore17, segment7.
RailwayContainer(railwayContainer1).
Sensor(sensor1).
Route(route1).
Region(region1).
Switch(switch2).
SwitchPosition(switchPosition1).
Sensor(sensor2).
Region(region2).
Sensor(sensor3).
Sensor(sensor4).
Sensor(sensor5).
Segment(segment1).
Sensor(sensor6).
Semaphore(semaphore1).
Region(region4).
Segment(segment2).
Region(region5).
Segment(segment3).
Semaphore(semaphore2).
Region(region6).
Switch(switch3).
Semaphore(semaphore3).
Region(region7).
Segment(segment4).
Semaphore(semaphore4).
Semaphore(semaphore6).
Semaphore(semaphore7).
Region(region8).
Semaphore(semaphore8).
Semaphore(semaphore9).
Semaphore(semaphore10).
Sensor(sensor7).
Sensor(sensor8).
Segment(segment5).
Switch(switch5).
Semaphore(semaphore11).
Semaphore(semaphore12).
Semaphore(semaphore13).
Semaphore(semaphore14).
Semaphore(semaphore15).
Sensor(sensor9).
Semaphore(semaphore16).
Region(region11).
Switch(switch6).
Segment(segment6).
Region(region13).
Sensor(sensor10).
Segment(segment7).
routes(railwayContainer1, route1).
regions(railwayContainer1, region1).
regions(railwayContainer1, region2).
regions(railwayContainer1, region3).
regions(railwayContainer1, region4).
regions(railwayContainer1, region5).
regions(railwayContainer1, region6).
regions(railwayContainer1, region7).
regions(railwayContainer1, region8).
regions(railwayContainer1, region9).
regions(railwayContainer1, region10).
regions(railwayContainer1, region11).
regions(railwayContainer1, region12).
regions(railwayContainer1, region13).
sensors(region1, sensor2).
sensors(region2, sensor1).
sensors(region2, sensor8).
sensors(region2, sensor10).
sensors(region3, sensor5).
sensors(region3, sensor6).
sensors(region4, sensor3).
sensors(region4, sensor7).
sensors(region5, sensor4).
elements(region1, switch2).
elements(region2, segment3).
elements(region3, segment2).
elements(region5, switch1).
elements(region6, switch3).
elements(region7, segment1).
elements(region9, segment4).
elements(region10, switch5).
elements(region12, segment7).
elements(region13, segment6).
follows(route1, switchPosition1).
semaphores(segment1, semaphore7).
semaphores(segment1, semaphore10).
semaphores(segment1, semaphore15).
semaphores(segment2, semaphore1).
semaphores(segment2, semaphore4).
semaphores(segment2, semaphore5).
semaphores(segment2, semaphore6).
semaphores(segment2, semaphore16).
semaphores(segment3, semaphore2).
semaphores(segment3, semaphore14).
semaphores(segment4, semaphore9).
semaphores(segment4, semaphore11).
semaphores(segment4, semaphore13).
semaphores(segment5, semaphore3).
semaphores(segment5, semaphore12).
semaphores(segment5, semaphore17).
signal(semaphore1, GO).
signal(semaphore2, GO).
signal(semaphore3, STOP).
signal(semaphore4, Signal::FAILURE).
signal(semaphore6, STOP).
signal(semaphore8, STOP).
signal(semaphore9, STOP).
signal(semaphore10, Signal::FAILURE).
signal(semaphore11, STOP).
signal(semaphore12, Signal::FAILURE).
signal(semaphore13, GO).
signal(semaphore14, Signal::FAILURE).
signal(semaphore15, GO).
signal(semaphore17, Signal::FAILURE).
requires(route1, sensor1).
requires(route1, sensor2).
requires(route1, sensor3).
requires(route1, sensor4).
requires(route1, sensor6).
requires(route1, sensor7).
requires(route1, sensor9).
entry(route1, semaphore1).
target(switchPosition1, switch5).
position(switchPosition1, Position::FAILURE).
monitoredBy(switch1, sensor2).
monitoredBy(switch1, sensor10).
monitoredBy(switch2, sensor1).
monitoredBy(switch2, sensor4).
monitoredBy(segment1, sensor3).
monitoredBy(segment1, sensor5).
monitoredBy(segment1, sensor8).
monitoredBy(segment2, sensor4).
monitoredBy(segment2, sensor6).
monitoredBy(segment2, sensor7).
monitoredBy(segment3, sensor1).
monitoredBy(segment3, sensor2).
monitoredBy(segment3, sensor4).
monitoredBy(segment3, sensor5).
monitoredBy(switch3, sensor3).
monitoredBy(switch3, sensor5).
monitoredBy(switch3, sensor8).
monitoredBy(segment4, sensor1).
monitoredBy(segment4, sensor2).
monitoredBy(segment4, sensor3).
monitoredBy(segment4, sensor5).
monitoredBy(switch4, sensor7).
monitoredBy(segment5, sensor2).
monitoredBy(segment5, sensor3).
monitoredBy(segment5, sensor4).
monitoredBy(segment5, sensor5).
monitoredBy(segment5, sensor8).
monitoredBy(switch5, sensor9).
monitoredBy(switch6, sensor6).
monitoredBy(segment6, sensor3).
monitoredBy(segment6, sensor10).
connectsTo(switch1, switch2).
connectsTo(switch1, segment2).
connectsTo(switch1, segment3).
connectsTo(switch1, switch3).
connectsTo(switch1, segment4).
connectsTo(switch1, switch4).
connectsTo(switch1, switch5).
connectsTo(switch2, segment1).
connectsTo(switch2, segment2).
connectsTo(switch2, segment3).
connectsTo(switch2, switch3).
connectsTo(switch2, segment4).
connectsTo(switch2, switch4).
connectsTo(switch2, switch5).
connectsTo(switch2, switch6).
connectsTo(segment1, switch1).
connectsTo(segment1, switch2).
connectsTo(segment2, switch1).
connectsTo(segment2, segment3).
connectsTo(segment3, segment1).
connectsTo(segment3, switch3).
connectsTo(switch3, switch1).
connectsTo(switch3, switch2).
connectsTo(switch3, segment1).
connectsTo(switch3, segment5).
connectsTo(switch3, switch5).
connectsTo(switch3, segment6).
connectsTo(segment4, switch3).
connectsTo(segment4, switch5).
connectsTo(switch4, switch2).
connectsTo(switch4, segment1).
connectsTo(switch4, segment2).
connectsTo(switch4, segment3).
connectsTo(switch4, switch3).
connectsTo(switch4, segment4).
connectsTo(switch4, segment5).
connectsTo(switch4, switch5).
connectsTo(switch4, segment6).
connectsTo(segment5, switch1).
connectsTo(switch5, switch1).
connectsTo(switch5, switch3).
connectsTo(switch5, segment4).
connectsTo(switch5, switch4).
connectsTo(switch5, segment5).
connectsTo(switch6, switch1).
connectsTo(switch6, segment1).
connectsTo(switch6, segment3).
connectsTo(switch6, switch3).
connectsTo(switch6, switch4).
connectsTo(switch6, switch5).
currentPosition(switch1, DIVERGING).
currentPosition(switch2, Position::FAILURE).
currentPosition(switch3, DIVERGING).
currentPosition(switch4, STRAIGHT).
currentPosition(switch5, Position::FAILURE).
currentPosition(switch6, Position::FAILURE).
straight(switch2, segment4).
straight(switch3, segment1).
straight(switch4, segment1).
straight(switch5, segment4).
straight(switch6, switch3).
diverging(switch1, segment3).
diverging(switch2, segment2).
diverging(switch3, segment3).
diverging(switch5, switch3).
diverging(switch6, segment1).
?active(route1).
?length(segment1).
?length(segment2).
?length(segment3).
?length(segment4).
?length(segment5).
?length(segment6).
?length(segment7).

default !connectsTo(*, *).
?connectsTo(*, Segment::new).
?connectsTo(Segment::new, *).

default !monitoredBy(*, *).
?monitoredBy(Sensor::new, *).
