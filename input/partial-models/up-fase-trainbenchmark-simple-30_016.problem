import builtin::view.

@color(hex="#ffffff")
abstract class TrackElement {
    TrackElement[1..3] connectsTo opposite connectsTo
    Sensor[] monitoredBy opposite monitors
}

@color(hex="#218380")
class Segment extends TrackElement {
}

@color(hex="#8f2d56")
class Switch extends TrackElement {
    TrackElement straight
    TrackElement diverging
}
class Sensor {
    TrackElement[] monitors opposite monitoredBy
}
class Region {
    contains Sensor[] sensors
    contains TrackElement[] elements
}


error sameStraightDiverging(switch, target) <->
    straight(switch,target),
    diverging(switch,target).

error noDiverging(switch) <->
    Switch(switch),
    !diverging(switch,_).

error noStraight(switch) <->
    Switch(switch),
    !straight(switch,_).

error tooManyConncetions(segment) <->
    Segment(segment),
    count{connectsTo(segment, _)} > 2.


error selfLoop(e) <->
    TrackElement(e),
    connectsTo(e, e)
;
    Switch(e),
    straight(e, e)
;
    Switch(e),
    diverging(e, e).


error underconnectedSwitch(sw) <->
    Switch(sw),
    !switchWithTwoConnections(sw, _, _).

pred switchWithTwoConnections(sw, te1, te2) <->
    Switch(sw),
    connectsTo(sw, te1),
    connectsTo(sw, te2),
    te1 != te2.


error sensorWithoutElement(sensor) <->
    Sensor(sensor),
    !monitors(sensor, _).

error sensorMonitorsMultipleSwitches(sensor, sw1, sw2) <->
    Sensor(sensor),
    monitors(sensor, sw1),
    monitors(sensor, sw2),
    Switch(sw1),
    Switch(sw2),
    sw1 != sw2.

error duplicateMonitors(region, sensor1, sensor2, te) <->
    Region(region),
    sensors(region, sensor1),
    sensors(region, sensor2),
    sensor1 != sensor2,
    monitors(sensor1, te),
    monitors(sensor2, te).

error emptyRegion(region) <->
    Region(region),
    !elements(region, _).


error connectedSegments(sensor, segment1, segment2, segment3, segment4, segment5, segment6) <->
    connectsTo(segment1, segment2),
    connectsTo(segment2, segment3),
    connectsTo(segment3, segment4),
    connectsTo(segment4, segment5),
    connectsTo(segment5, segment6),
    monitoredBy(segment1, sensor),
    monitoredBy(segment2, sensor),
    monitoredBy(segment3, sensor),
    monitoredBy(segment4, sensor),
    monitoredBy(segment5, sensor),
    monitoredBy(segment6, sensor).


error switchMonitored(sw) <-> 
    Switch(sw),
    !monitoredBy(sw, _).

declare switch1, region1, region2, sensor1, sensor2, sensor3, sensor4,
	sensor5, sensor6, region3, sensor7, segment1, segment2, segment3, sensor8,
	region4, sensor9, region5, sensor10, sensor11, segment4, sensor12,
	sensor13, region6, sensor14, sensor15, sensor16, sensor17, sensor18,
	sensor19, segment5, segment6, switch2, switch3, switch4, sensor20.
Switch(switch1).
Region(region1).
Region(region2).
Sensor(sensor1).
Sensor(sensor2).
Sensor(sensor3).
Sensor(sensor4).
Sensor(sensor5).
Sensor(sensor6).
Region(region3).
Sensor(sensor7).
Segment(segment1).
Segment(segment2).
Segment(segment3).
Sensor(sensor8).
Region(region4).
Sensor(sensor9).
Region(region5).
Sensor(sensor10).
Sensor(sensor11).
Segment(segment4).
Sensor(sensor12).
Sensor(sensor13).
Region(region6).
Sensor(sensor14).
Sensor(sensor15).
Sensor(sensor17).
Sensor(sensor18).
Sensor(sensor19).
Segment(segment5).
Segment(segment6).
Switch(switch2).
Switch(switch3).
Switch(switch4).
Sensor(sensor20).
sensors(region1, sensor3).
sensors(region1, sensor5).
sensors(region1, sensor6).
sensors(region1, sensor11).
sensors(region1, sensor12).
sensors(region1, sensor13).
sensors(region2, sensor1).
sensors(region2, sensor2).
sensors(region2, sensor4).
sensors(region2, sensor7).
sensors(region3, sensor8).
sensors(region3, sensor9).
sensors(region3, sensor10).
sensors(region3, sensor17).
sensors(region4, sensor18).
sensors(region4, sensor19).
sensors(region5, sensor20).
sensors(region6, sensor14).
elements(region2, segment2).
elements(region2, switch2).
elements(region2, switch4).
elements(region3, segment1).
elements(region4, segment4).
elements(region4, switch3).
elements(region5, segment5).
elements(region6, segment6).
monitoredBy(switch1, sensor16).
monitoredBy(switch1, sensor20).
monitoredBy(segment1, sensor2).
monitoredBy(segment1, sensor6).
monitoredBy(segment1, sensor8).
monitoredBy(segment1, sensor14).
monitoredBy(segment1, sensor18).
monitoredBy(segment2, sensor1).
monitoredBy(segment2, sensor16).
monitoredBy(segment2, sensor19).
monitoredBy(segment2, sensor20).
monitoredBy(segment3, sensor6).
monitoredBy(segment3, sensor10).
monitoredBy(segment3, sensor15).
monitoredBy(segment3, sensor18).
monitoredBy(segment4, sensor5).
monitoredBy(segment4, sensor7).
monitoredBy(segment4, sensor10).
monitoredBy(segment4, sensor14).
monitoredBy(segment4, sensor18).
monitoredBy(segment5, sensor4).
monitoredBy(segment5, sensor13).
monitoredBy(segment5, sensor19).
monitoredBy(segment5, sensor20).
monitoredBy(segment6, sensor4).
monitoredBy(segment6, sensor11).
monitoredBy(segment6, sensor16).
monitoredBy(segment6, sensor17).
monitoredBy(segment6, sensor19).
monitoredBy(segment6, sensor20).
monitoredBy(switch2, sensor3).
monitoredBy(switch2, sensor10).
monitoredBy(switch2, sensor15).
monitoredBy(switch3, sensor2).
monitoredBy(switch3, sensor11).
monitoredBy(switch3, sensor17).
monitoredBy(switch3, sensor19).
monitoredBy(switch4, sensor7).
monitoredBy(switch4, sensor12).
monitoredBy(switch4, sensor14).
straight(switch1, segment1).
straight(switch2, segment6).
straight(switch3, segment6).
straight(switch4, segment1).
diverging(switch1, segment2).
diverging(switch2, segment2).
connectsTo(switch1, segment1).
connectsTo(switch1, segment3).
connectsTo(segment1, switch1).
connectsTo(segment2, switch2).
connectsTo(segment2, switch4).
connectsTo(segment3, switch1).
connectsTo(segment3, switch3).
connectsTo(segment5, switch2).
connectsTo(segment5, switch4).
connectsTo(segment6, switch2).
connectsTo(segment6, switch4).
connectsTo(switch2, segment2).
connectsTo(switch2, segment5).
connectsTo(switch2, segment6).
connectsTo(switch3, segment3).
connectsTo(switch3, segment4).
connectsTo(switch4, segment2).
connectsTo(switch4, segment5).
connectsTo(switch4, segment6).

default !connectsTo(*, *).
?connectsTo(*, Segment::new).
?connectsTo(Segment::new, *).

default !monitoredBy(*, *).
?monitoredBy(*, Segment::new).
?monitoredBy(Sensor::new, *).
