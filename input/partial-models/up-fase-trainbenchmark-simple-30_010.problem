import builtin::view.

@color(hex="#ffffff")
abstract class TrackElement {
    TrackElement[1..3] connectsTo opposite connectsTo
    Sensor[] monitoredBy opposite monitors
}

@color(hex="#218380")
class Segment extends TrackElement {
}

@color(hex="#8f2d56")
class Switch extends TrackElement {
    TrackElement straight
    TrackElement diverging
}
class Sensor {
    TrackElement[] monitors opposite monitoredBy
}
class Region {
    contains Sensor[] sensors
    contains TrackElement[] elements
}


error sameStraightDiverging(switch, target) <->
    straight(switch,target),
    diverging(switch,target).

error noDiverging(switch) <->
    Switch(switch),
    !diverging(switch,_).

error noStraight(switch) <->
    Switch(switch),
    !straight(switch,_).

error tooManyConncetions(segment) <->
    Segment(segment),
    count{connectsTo(segment, _)} > 2.


error selfLoop(e) <->
    TrackElement(e),
    connectsTo(e, e)
;
    Switch(e),
    straight(e, e)
;
    Switch(e),
    diverging(e, e).


error underconnectedSwitch(sw) <->
    Switch(sw),
    !switchWithTwoConnections(sw, _, _).

pred switchWithTwoConnections(sw, te1, te2) <->
    Switch(sw),
    connectsTo(sw, te1),
    connectsTo(sw, te2),
    te1 != te2.


error sensorWithoutElement(sensor) <->
    Sensor(sensor),
    !monitors(sensor, _).

error sensorMonitorsMultipleSwitches(sensor, sw1, sw2) <->
    Sensor(sensor),
    monitors(sensor, sw1),
    monitors(sensor, sw2),
    Switch(sw1),
    Switch(sw2),
    sw1 != sw2.

error duplicateMonitors(region, sensor1, sensor2, te) <->
    Region(region),
    sensors(region, sensor1),
    sensors(region, sensor2),
    sensor1 != sensor2,
    monitors(sensor1, te),
    monitors(sensor2, te).

error emptyRegion(region) <->
    Region(region),
    !elements(region, _).


error connectedSegments(sensor, segment1, segment2, segment3, segment4, segment5, segment6) <->
    connectsTo(segment1, segment2),
    connectsTo(segment2, segment3),
    connectsTo(segment3, segment4),
    connectsTo(segment4, segment5),
    connectsTo(segment5, segment6),
    monitoredBy(segment1, sensor),
    monitoredBy(segment2, sensor),
    monitoredBy(segment3, sensor),
    monitoredBy(segment4, sensor),
    monitoredBy(segment5, sensor),
    monitoredBy(segment6, sensor).


error switchMonitored(sw) <-> 
    Switch(sw),
    !monitoredBy(sw, _).

declare region1, segment1, sensor1, region2, sensor2, segment2, sensor3,
	region3, sensor4, region4, segment3, sensor5, sensor6, region5, sensor7,
	segment4, sensor8, region6, region7, segment5, segment6, sensor9,
	sensor10, segment7, sensor11, sensor12, sensor13, sensor14, segment8,
	switch1, switch2, switch3, segment9.
Region(region1).
Segment(segment1).
Sensor(sensor1).
Region(region2).
Segment(segment2).
Sensor(sensor3).
Region(region3).
Sensor(sensor4).
Region(region4).
Segment(segment3).
Sensor(sensor5).
Sensor(sensor6).
Region(region5).
Sensor(sensor7).
Segment(segment4).
Sensor(sensor8).
Region(region6).
Region(region7).
Segment(segment5).
Segment(segment6).
Sensor(sensor9).
Sensor(sensor10).
Segment(segment7).
Sensor(sensor12).
Sensor(sensor13).
Sensor(sensor14).
Segment(segment8).
Switch(switch1).
Switch(switch2).
Switch(switch3).
Segment(segment9).
sensors(region1, sensor1).
sensors(region1, sensor3).
sensors(region2, sensor2).
sensors(region2, sensor5).
sensors(region2, sensor8).
sensors(region2, sensor11).
sensors(region2, sensor12).
sensors(region3, sensor4).
sensors(region4, sensor6).
sensors(region4, sensor14).
sensors(region5, sensor10).
sensors(region6, sensor9).
sensors(region6, sensor13).
elements(region1, segment1).
elements(region2, segment2).
elements(region2, switch2).
elements(region3, segment9).
elements(region4, segment3).
elements(region4, segment7).
elements(region5, segment4).
elements(region5, segment5).
elements(region6, segment8).
elements(region7, switch3).
monitoredBy(segment1, sensor1).
monitoredBy(segment1, sensor2).
monitoredBy(segment1, sensor4).
monitoredBy(segment1, sensor6).
monitoredBy(segment1, sensor9).
monitoredBy(segment2, sensor2).
monitoredBy(segment2, sensor3).
monitoredBy(segment2, sensor6).
monitoredBy(segment2, sensor7).
monitoredBy(segment3, sensor1).
monitoredBy(segment3, sensor2).
monitoredBy(segment3, sensor4).
monitoredBy(segment3, sensor6).
monitoredBy(segment3, sensor9).
monitoredBy(segment3, sensor10).
monitoredBy(segment4, sensor2).
monitoredBy(segment4, sensor3).
monitoredBy(segment4, sensor7).
monitoredBy(segment4, sensor10).
monitoredBy(segment4, sensor13).
monitoredBy(segment4, sensor14).
monitoredBy(segment5, sensor1).
monitoredBy(segment5, sensor5).
monitoredBy(segment5, sensor7).
monitoredBy(segment5, sensor13).
monitoredBy(segment6, sensor1).
monitoredBy(segment6, sensor6).
monitoredBy(segment6, sensor9).
monitoredBy(segment6, sensor11).
monitoredBy(segment7, sensor3).
monitoredBy(segment7, sensor7).
monitoredBy(segment7, sensor12).
monitoredBy(segment7, sensor13).
monitoredBy(segment7, sensor14).
monitoredBy(segment8, sensor3).
monitoredBy(segment8, sensor6).
monitoredBy(segment8, sensor7).
monitoredBy(segment8, sensor8).
monitoredBy(segment8, sensor9).
monitoredBy(switch1, sensor10).
monitoredBy(switch1, sensor12).
monitoredBy(switch1, sensor13).
monitoredBy(switch1, sensor14).
monitoredBy(switch2, sensor4).
monitoredBy(switch3, sensor11).
monitoredBy(segment9, sensor10).
straight(switch2, segment7).
straight(switch3, segment8).
diverging(switch1, segment5).
diverging(switch2, segment8).
diverging(switch3, segment3).
connectsTo(segment1, switch1).
connectsTo(segment2, switch1).
connectsTo(segment3, switch3).
connectsTo(segment4, switch3).
connectsTo(segment5, switch2).
connectsTo(segment6, switch2).
connectsTo(segment7, switch2).
connectsTo(segment8, switch1).
connectsTo(switch1, segment1).
connectsTo(switch1, segment2).
connectsTo(switch1, segment8).
connectsTo(switch2, segment5).
connectsTo(switch3, segment3).
connectsTo(switch3, segment4).
connectsTo(switch3, segment9).
connectsTo(segment9, switch3).

default !connectsTo(*, *).
?connectsTo(*, Segment::new).
?connectsTo(Segment::new, *).

default !monitoredBy(*, *).
?monitoredBy(*, Segment::new).
?monitoredBy(Sensor::new, *).
