
abstract class CompositeElement {
    contains Region[] regions
}

class Region {
    contains Vertex[] vertices opposite region
}

abstract class Vertex {
    container Region region opposite vertices
    contains Transition[] outgoingTransition opposite source
    Transition[] incomingTransition opposite target
}

class Transition {
    container Vertex source opposite outgoingTransition
    Vertex[1] target opposite incomingTransition
}

abstract class Pseudostate extends Vertex.

abstract class RegularState extends Vertex.

class Entry extends Pseudostate.

class Exit extends Pseudostate.

class Choice extends Pseudostate.

class FinalState extends RegularState.

class State extends RegularState, CompositeElement.

class Statechart extends CompositeElement.



pred entryInRegion(r, Entry e) <->
    vertices(r, e).

error noEntryInRegion(Region r) <->
    !entryInRegion(r, _).

error multipleEntryInRegion(r) <->
    Entry(e1),
    Entry(e2),
    vertices(r, e1),
    vertices(r, e2),
    e1 != e2.

error incomingToEntry(t, Entry e) <->
    target(t, e).

error noOutgoingTransitionFromEntry(Entry e) <->
    !source(_, e).

error multipleTransitionFromEntry(Entry e, t1, t2) <->
    outgoingTransition(e, t1),
    outgoingTransition(e, t2),
    t1 != t2.


error outgoingFromExit(t, Exit e) <->
    source(t, e).


error outgoingFromFinal(t, FinalState e) <->
    source(t, e).


pred stateInRegion(r, State s) <->
    vertices(r, s).

error noStateInRegion(Region r) <->
    !stateInRegion(r, _).


error choiceHasNoOutgoing(Choice c) <->
    !source(_, c).

error choiceHasNoIncoming(Choice c) <->
    !target(_, c).

declare sct, transition1, region1, finalState1, region2, finalState2, exit1,
	choice1, transition2, state1, state2, transition3, transition4,
	finalState3, entry1, exit2, entry2, transition5, exit3, exit4,
	transition6, state3, exit5, transition7.
declare entry3, finalState4, transition8, region3, state4, state5.
declare exit6, transition9, state6.
declare transition10, region4, exit7, state7, finalState5, state8, entry4,
	transition11, finalState6.
declare finalState7, transition12, exit8, finalState8, exit9, state9.
FinalState(finalState7).
Statechart(sct).
Statechart(transition1).
Choice(finalState1).
Region(region2).
FinalState(finalState2).
Exit(exit1).
Choice(choice1).
Transition(transition2).
Choice(state1).
State(state2).
Transition(transition3).
Transition(transition4).
FinalState(finalState3).
Entry(entry1).
State(exit2).
Entry(entry2).
Transition(transition5).
Exit(exit3).
Exit(exit4).
Transition(transition6).
State(state3).
Exit(exit5).
Transition(transition7).
Entry(entry3).
FinalState(finalState4).
Transition(transition8).
Region(region3).
Exit(state4).
FinalState(state5).
FinalState(exit6).
Transition(transition9).
State(state6).
Region(region4).
Exit(exit7).
State(state7).
FinalState(finalState5).
State(state8).
Entry(entry4).
FinalState(finalState6).
Transition(transition12).
Exit(exit8).
FinalState(finalState8).
Exit(exit9).
State(state9).
regions(sct, region2).
regions(sct, region3).
regions(sct, region4).
regions(transition1, region1).
vertices(region1, exit1).
vertices(region1, state1).
vertices(region1, state2).
vertices(region1, finalState3).
vertices(region1, exit2).
vertices(region1, entry2).
vertices(region1, exit3).
vertices(region1, finalState4).
vertices(region1, exit7).
vertices(region1, finalState5).
vertices(region1, state8).
vertices(region1, finalState8).
vertices(region2, finalState1).
vertices(region2, finalState2).
vertices(region2, entry1).
vertices(region2, exit4).
vertices(region2, state3).
vertices(region2, state4).
vertices(region2, exit6).
vertices(region2, exit8).
vertices(region3, exit5).
vertices(region3, entry3).
vertices(region3, state5).
vertices(region3, state6).
vertices(region3, exit9).
vertices(region4, entry4).
vertices(region4, finalState6).
outgoingTransition(finalState1, transition8).
outgoingTransition(choice1, transition4).
outgoingTransition(choice1, transition5).
outgoingTransition(state1, transition6).
outgoingTransition(entry1, transition7).
outgoingTransition(exit2, transition12).
outgoingTransition(entry2, transition9).
outgoingTransition(entry3, transition2).
outgoingTransition(state6, transition10).
outgoingTransition(entry4, transition11).
incomingTransition(choice1, transition5).
incomingTransition(state1, transition3).
incomingTransition(state1, transition9).
incomingTransition(finalState3, transition12).
incomingTransition(exit4, transition7).
incomingTransition(exit5, transition2).
incomingTransition(state5, transition8).
incomingTransition(exit6, transition6).
