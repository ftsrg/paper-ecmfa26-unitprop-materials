import builtin::view.

@color(hex="#ffffff")
abstract class TrackElement {
    TrackElement[1..3] connectsTo opposite connectsTo
}

@color(hex="#218380")
class Segment extends TrackElement {
}

@color(hex="#8f2d56")
class Switch extends TrackElement {
    TrackElement straight
    TrackElement diverging
}


error sameStraightDiverging(switch, target) <->
    straight(switch,target),
    diverging(switch,target).

error noDiverging(switch) <->
    Switch(switch),
    !diverging(switch,_).

error noStraight(switch) <->
    Switch(switch),
    !straight(switch,_).

error tooManyConncetions(segment) <->
    Segment(segment),
    count{connectsTo(segment, _)} > 2.


error selfLoop(e) <->
    TrackElement(e),
    connectsTo(e, e)
;
    Switch(e),
    straight(e, e)
;
    Switch(e),
    diverging(e, e).
    

error underconnectedSwitch(sw) <->
    Switch(sw),
    !switchWithTwoConnections(sw, _, _).

pred switchWithTwoConnections(sw, te1, te2) <->
    Switch(sw),
    connectsTo(sw, te1),
    connectsTo(sw, te2),
    te1 != te2.

declare switch1, segment1, segment2, segment3, segment4, switch2, segment5,
	segment6, segment7, segment8, switch3, segment9, segment10, segment11,
	segment12, segment13, segment14, segment15, segment16, segment17, switch4,
	switch5, switch6, switch7.
Switch(switch1).
Segment(segment3).
Segment(segment4).
Switch(switch2).
Segment(segment5).
Segment(segment6).
Segment(segment7).
Segment(segment8).
Switch(switch3).
Segment(segment9).
Segment(segment10).
Segment(segment11).
Segment(segment12).
Segment(segment13).
Segment(segment14).
Segment(segment15).
Segment(segment16).
Switch(switch4).
Switch(switch5).
Switch(switch6).
Switch(switch7).
straight(switch1, segment3).
straight(switch2, segment2).
straight(switch4, segment11).
straight(switch5, segment10).
straight(switch7, segment11).
diverging(switch1, segment15).
diverging(switch2, segment5).
diverging(switch3, segment9).
diverging(switch4, segment3).
diverging(switch5, segment7).
diverging(switch6, segment16).
diverging(switch7, segment4).
connectsTo(switch1, segment9).
connectsTo(switch1, segment11).
connectsTo(segment2, switch2).
connectsTo(segment3, switch2).
connectsTo(segment3, switch5).
connectsTo(segment4, switch4).
connectsTo(segment4, switch5).
connectsTo(switch2, segment1).
connectsTo(switch2, segment2).
connectsTo(switch2, segment3).
connectsTo(segment5, switch3).
connectsTo(segment6, switch4).
connectsTo(segment7, switch3).
connectsTo(segment8, switch7).
connectsTo(switch3, segment2).
connectsTo(switch3, segment7).
connectsTo(segment9, switch1).
connectsTo(segment10, switch7).
connectsTo(segment11, switch1).
connectsTo(segment12, switch6).
connectsTo(segment13, switch6).
connectsTo(segment14, switch7).
connectsTo(segment15, switch4).
connectsTo(segment16, switch5).
connectsTo(segment17, switch6).
connectsTo(switch4, segment4).
connectsTo(switch4, segment6).
connectsTo(switch4, segment15).
connectsTo(switch5, segment3).
connectsTo(switch5, segment4).
connectsTo(switch5, segment16).
connectsTo(switch6, segment12).
connectsTo(switch6, segment13).
connectsTo(switch6, segment17).
connectsTo(switch7, segment8).
connectsTo(switch7, segment10).
connectsTo(switch7, segment14).
