
abstract class CompositeElement {
    contains Region[] regions
}

class Region {
    contains Vertex[] vertices opposite region
}

abstract class Vertex {
    container Region region opposite vertices
    contains Transition[] outgoingTransition opposite source
    Transition[] incomingTransition opposite target
}

class Transition {
    container Vertex source opposite outgoingTransition
    Vertex[1] target opposite incomingTransition
}

abstract class Pseudostate extends Vertex.

abstract class RegularState extends Vertex.

class Entry extends Pseudostate.

class Exit extends Pseudostate.

class Choice extends Pseudostate.

class FinalState extends RegularState.

class State extends RegularState, CompositeElement.

class Statechart extends CompositeElement.



pred entryInRegion(r, Entry e) <->
    vertices(r, e).

error noEntryInRegion(Region r) <->
    !entryInRegion(r, _).

error multipleEntryInRegion(r) <->
    Entry(e1),
    Entry(e2),
    vertices(r, e1),
    vertices(r, e2),
    e1 != e2.

error incomingToEntry(t, Entry e) <->
    target(t, e).

error noOutgoingTransitionFromEntry(Entry e) <->
    !source(_, e).

error multipleTransitionFromEntry(Entry e, t1, t2) <->
    outgoingTransition(e, t1),
    outgoingTransition(e, t2),
    t1 != t2.


error outgoingFromExit(t, Exit e) <->
    source(t, e).


error outgoingFromFinal(t, FinalState e) <->
    source(t, e).


pred stateInRegion(r, State s) <->
    vertices(r, s).

error noStateInRegion(Region r) <->
    !stateInRegion(r, _).


error choiceHasNoOutgoing(Choice c) <->
    !source(_, c).

error choiceHasNoIncoming(Choice c) <->
    !target(_, c).

declare sct, transition1, region1, state1, exit1, region2, state2, exit2,
	finalState1, transition2, state3, transition3, exit3, finalState2,
	choice1, entry1, entry2, finalState3, transition4, transition5, state4,
	transition6, finalState4, finalState5.
declare transition7, region3, choice2, transition8, entry3, transition9.
declare finalState6, finalState7, state5.
declare transition10, exit4, region4, state6, state7, transition11,
	finalState8, entry4, state8.
declare transition12, choice3, exit5, finalState9, region5, finalState10,
	transition13, state9, entry5, finalState11, transition14, exit6.
declare transition15, region6, state10, finalState12, entry6, transition16.
declare transition17, state11, exit7, exit8.
declare transition18, state12, region7, state13, exit9, state14, state15,
	entry7, exit10, transition19, finalState13, transition20, exit11, state16.
declare exit12, state17.
declare transition21, finalState14, state18, state19, state20, transition22,
	state21, finalState15, finalState16, entry8.
Statechart(sct).
Transition(transition1).
Region(region1).
State(state1).
Exit(exit1).
Region(region2).
State(state2).
Exit(exit2).
FinalState(finalState1).
Transition(transition2).
Exit(state3).
Transition(transition3).
Exit(exit3).
FinalState(finalState2).
Choice(choice1).
Entry(entry1).
Entry(entry2).
State(finalState3).
Transition(transition4).
Transition(transition5).
State(state4).
Transition(transition6).
State(finalState4).
Region(finalState5).
Transition(transition7).
Region(region3).
Choice(choice2).
Transition(transition8).
Entry(entry3).
Transition(transition9).
Exit(finalState6).
State(state5).
Transition(transition10).
Exit(exit4).
Region(region4).
State(state6).
State(state7).
Transition(transition11).
Exit(finalState8).
Entry(entry4).
State(state8).
Transition(transition12).
Choice(choice3).
Exit(exit5).
FinalState(finalState9).
Region(region5).
FinalState(finalState10).
Transition(transition13).
State(state9).
FinalState(finalState11).
Exit(exit6).
Transition(transition15).
Region(region6).
Exit(finalState12).
Entry(entry6).
Transition(transition16).
State(state11).
State(exit7).
Transition(transition18).
State(state12).
Region(region7).
State(state13).
Exit(exit9).
State(state14).
State(state15).
Entry(entry7).
Exit(exit10).
FinalState(finalState13).
Transition(transition20).
Exit(exit11).
Exit(exit12).
State(state17).
Transition(transition21).
FinalState(finalState14).
State(state18).
State(state20).
Transition(transition22).
State(state21).
FinalState(finalState15).
FinalState(finalState16).
Entry(entry8).
regions(sct, region2).
regions(state1, region6).
regions(state2, region5).
regions(state4, region3).
regions(state4, region7).
regions(state11, region1).
regions(state12, region4).
regions(state15, finalState5).
vertices(region1, state1).
vertices(region1, exit1).
vertices(region1, exit2).
vertices(region1, state3).
vertices(region1, finalState2).
vertices(region1, entry2).
vertices(region1, exit4).
vertices(region1, exit6).
vertices(region1, exit7).
vertices(region2, state2).
vertices(region2, finalState1).
vertices(region2, entry1).
vertices(region2, state4).
vertices(region2, finalState6).
vertices(region2, finalState7).
vertices(region2, state5).
vertices(region2, finalState8).
vertices(region2, exit8).
vertices(finalState5, state19).
vertices(region3, choice1).
vertices(region3, finalState4).
vertices(region3, entry3).
vertices(region3, choice3).
vertices(region3, state11).
vertices(region3, exit12).
vertices(region3, state21).
vertices(region4, exit3).
vertices(region4, state7).
vertices(region4, entry4).
vertices(region4, state8).
vertices(region4, exit5).
vertices(region4, exit11).
vertices(region5, finalState11).
vertices(region5, finalState12).
vertices(region5, state17).
vertices(region5, finalState14).
vertices(region6, choice2).
vertices(region6, finalState10).
vertices(region6, entry5).
vertices(region6, exit9).
vertices(region6, state15).
vertices(region7, finalState3).
vertices(region7, state9).
vertices(region7, state12).
vertices(region7, state13).
vertices(region7, exit10).
vertices(region7, state16).
vertices(region7, finalState15).
vertices(region7, entry8).
outgoingTransition(state1, transition2).
outgoingTransition(state1, transition11).
outgoingTransition(state1, transition12).
outgoingTransition(state1, transition17).
outgoingTransition(state2, transition1).
outgoingTransition(choice1, transition6).
outgoingTransition(entry1, transition8).
outgoingTransition(entry2, transition5).
outgoingTransition(state4, transition3).
outgoingTransition(state4, transition19).
outgoingTransition(choice2, transition9).
outgoingTransition(entry3, transition7).
outgoingTransition(entry4, transition10).
outgoingTransition(state8, transition20).
outgoingTransition(choice3, transition13).
outgoingTransition(choice3, transition15).
outgoingTransition(entry5, transition14).
outgoingTransition(state10, transition18).
outgoingTransition(entry6, transition16).
outgoingTransition(state14, transition4).
outgoingTransition(entry7, transition22).
outgoingTransition(entry8, transition21).
incomingTransition(exit1, transition4).
incomingTransition(exit2, transition13).
incomingTransition(finalState1, transition8).
incomingTransition(state3, transition6).
incomingTransition(finalState2, transition5).
incomingTransition(choice1, transition1).
incomingTransition(choice1, transition9).
incomingTransition(finalState4, transition2).
incomingTransition(finalState4, transition7).
incomingTransition(choice2, transition11).
incomingTransition(choice2, transition16).
incomingTransition(state8, transition10).
incomingTransition(choice3, transition12).
incomingTransition(state9, transition17).
incomingTransition(exit6, transition18).
incomingTransition(state10, transition20).
incomingTransition(finalState12, transition14).
incomingTransition(state13, transition19).
incomingTransition(finalState14, transition21).
incomingTransition(finalState15, transition22).
