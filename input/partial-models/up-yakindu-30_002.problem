
abstract class CompositeElement {
    contains Region[] regions
}

class Region {
    contains Vertex[] vertices opposite region
}

abstract class Vertex {
    container Region region opposite vertices
    contains Transition[] outgoingTransition opposite source
    Transition[] incomingTransition opposite target
}

class Transition {
    container Vertex source opposite outgoingTransition
    Vertex[1] target opposite incomingTransition
}

abstract class Pseudostate extends Vertex.

abstract class RegularState extends Vertex.

class Entry extends Pseudostate.

class Exit extends Pseudostate.

class Choice extends Pseudostate.

class FinalState extends RegularState.

class State extends RegularState, CompositeElement.

class Statechart extends CompositeElement.



pred entryInRegion(r, Entry e) <->
    vertices(r, e).

error noEntryInRegion(Region r) <->
    !entryInRegion(r, _).

error multipleEntryInRegion(r) <->
    Entry(e1),
    Entry(e2),
    vertices(r, e1),
    vertices(r, e2),
    e1 != e2.

error incomingToEntry(t, Entry e) <->
    target(t, e).

error noOutgoingTransitionFromEntry(Entry e) <->
    !source(_, e).

error multipleTransitionFromEntry(Entry e, t1, t2) <->
    outgoingTransition(e, t1),
    outgoingTransition(e, t2),
    t1 != t2.


error outgoingFromExit(t, Exit e) <->
    source(t, e).


error outgoingFromFinal(t, FinalState e) <->
    source(t, e).


pred stateInRegion(r, State s) <->
    vertices(r, s).

error noStateInRegion(Region r) <->
    !stateInRegion(r, _).


error choiceHasNoOutgoing(Choice c) <->
    !source(_, c).

error choiceHasNoIncoming(Choice c) <->
    !target(_, c).

declare sct, transition1, region1, region2, choice1, transition2, state1,
	transition3, region3, transition4, choice2, finalState1, exit1,
	finalState2, finalState3, state2, finalState4, finalState5, entry1,
	transition5, transition6, state3, exit2, entry2, entry3, finalState6,
	state4, state5, finalState7, transition7, transition8, state6, state7,
	finalState8, transition9, finalState9.
Statechart(sct).
Region(region1).
Region(region2).
Transition(transition2).
State(state1).
Transition(transition3).
Region(region3).
Transition(transition4).
Choice(choice2).
FinalState(finalState1).
Exit(exit1).
FinalState(finalState2).
FinalState(finalState3).
State(state2).
FinalState(finalState4).
FinalState(finalState5).
Entry(entry1).
State(state3).
Exit(exit2).
Entry(entry2).
Entry(entry3).
FinalState(finalState6).
State(state4).
State(state5).
FinalState(finalState7).
Transition(transition7).
Transition(transition8).
State(state6).
State(state7).
FinalState(finalState8).
Transition(transition9).
FinalState(finalState9).
regions(sct, region1).
regions(sct, region2).
regions(state1, region3).
vertices(region1, state1).
vertices(region1, finalState1).
vertices(region1, exit1).
vertices(region1, state2).
vertices(region1, entry3).
vertices(region1, state4).
vertices(region1, finalState8).
vertices(region2, choice1).
vertices(region2, choice2).
vertices(region2, finalState3).
vertices(region2, finalState6).
vertices(region2, state6).
vertices(region2, state7).
vertices(region3, finalState4).
vertices(region3, finalState5).
vertices(region3, entry1).
vertices(region3, state3).
vertices(region3, finalState7).
vertices(region3, finalState9).
outgoingTransition(choice1, transition2).
outgoingTransition(choice1, transition4).
outgoingTransition(state1, transition7).
outgoingTransition(choice2, transition9).
outgoingTransition(state2, transition5).
outgoingTransition(entry1, transition6).
outgoingTransition(entry2, transition1).
outgoingTransition(entry3, transition8).
incomingTransition(choice1, transition2).
incomingTransition(choice1, transition3).
incomingTransition(choice1, transition4).
incomingTransition(choice2, transition1).
incomingTransition(exit1, transition6).
incomingTransition(exit1, transition7).
incomingTransition(finalState3, transition8).
incomingTransition(finalState5, transition9).
incomingTransition(exit2, transition5).
