import builtin::view.

@color(hex="#ffffff")
abstract class TrackElement {
    TrackElement[1..3] connectsTo opposite connectsTo
    Sensor[] monitoredBy opposite monitors
}

@color(hex="#218380")
class Segment extends TrackElement {
}

@color(hex="#8f2d56")
class Switch extends TrackElement {
    TrackElement straight
    TrackElement diverging
}
class Sensor {
    TrackElement[] monitors opposite monitoredBy
}
class Region {
    contains Sensor[] sensors
    contains TrackElement[] elements
}


error sameStraightDiverging(switch, target) <->
    straight(switch,target),
    diverging(switch,target).

error noDiverging(switch) <->
    Switch(switch),
    !diverging(switch,_).

error noStraight(switch) <->
    Switch(switch),
    !straight(switch,_).

error tooManyConncetions(segment) <->
    Segment(segment),
    count{connectsTo(segment, _)} > 2.


error selfLoop(e) <->
    TrackElement(e),
    connectsTo(e, e)
;
    Switch(e),
    straight(e, e)
;
    Switch(e),
    diverging(e, e).


error underconnectedSwitch(sw) <->
    Switch(sw),
    !switchWithTwoConnections(sw, _, _).

pred switchWithTwoConnections(sw, te1, te2) <->
    Switch(sw),
    connectsTo(sw, te1),
    connectsTo(sw, te2),
    te1 != te2.


error sensorWithoutElement(sensor) <->
    Sensor(sensor),
    !monitors(sensor, _).

error sensorMonitorsMultipleSwitches(sensor, sw1, sw2) <->
    Sensor(sensor),
    monitors(sensor, sw1),
    monitors(sensor, sw2),
    Switch(sw1),
    Switch(sw2),
    sw1 != sw2.

error duplicateMonitors(region, sensor1, sensor2, te) <->
    Region(region),
    sensors(region, sensor1),
    sensors(region, sensor2),
    sensor1 != sensor2,
    monitors(sensor1, te),
    monitors(sensor2, te).

error emptyRegion(region) <->
    Region(region),
    !elements(region, _).


error connectedSegments(sensor, segment1, segment2, segment3, segment4, segment5, segment6) <->
    connectsTo(segment1, segment2),
    connectsTo(segment2, segment3),
    connectsTo(segment3, segment4),
    connectsTo(segment4, segment5),
    connectsTo(segment5, segment6),
    monitoredBy(segment1, sensor),
    monitoredBy(segment2, sensor),
    monitoredBy(segment3, sensor),
    monitoredBy(segment4, sensor),
    monitoredBy(segment5, sensor),
    monitoredBy(segment6, sensor).


error switchMonitored(sw) <-> 
    Switch(sw),
    !monitoredBy(sw, _).

declare sensor1, region1, segment1, sensor2, region2, sensor3, region3,
	segment2, segment3, segment4, segment5, sensor4, region4, region5,
	sensor5, segment6, sensor6, region6, region7, segment7, switch1, switch2,
	segment8, switch3.
declare sensor7.
declare segment9, sensor8, sensor9, sensor10, segment10.
declare switch4, sensor11, segment11, sensor12, segment12.
declare segment13, segment14, sensor13, sensor14, sensor15.
declare segment15, sensor16, switch5, region8, sensor17, segment16.
declare segment17, sensor18, segment18, segment19.
declare switch6, segment20, segment21, segment22, sensor19.
declare segment23, sensor20, segment24, sensor21, segment25.
declare sensor22, switch7, sensor23, sensor24, sensor25.
declare segment26, segment27, sensor26, sensor27, sensor28.
declare switch8, segment28, segment29, sensor29, sensor30.
declare sensor31, sensor32, sensor33, sensor34, sensor35.
Sensor(sensor1).
Region(region1).
Segment(segment1).
Region(region2).
Sensor(sensor3).
Region(region3).
Segment(segment2).
Segment(segment3).
Segment(segment4).
Segment(segment5).
Region(region4).
Region(region5).
Sensor(sensor5).
Segment(segment6).
Sensor(sensor6).
Region(region6).
Region(region7).
Segment(segment7).
Switch(switch2).
Segment(segment8).
Switch(switch3).
Sensor(sensor7).
Segment(segment9).
Sensor(sensor8).
Sensor(sensor9).
Sensor(sensor10).
Segment(segment10).
Switch(switch4).
Sensor(sensor11).
Sensor(sensor12).
Segment(segment13).
Segment(segment14).
Sensor(sensor13).
Sensor(sensor14).
Sensor(sensor15).
Segment(segment15).
Sensor(sensor16).
Switch(switch5).
Sensor(sensor17).
Segment(segment16).
Segment(segment17).
Sensor(sensor18).
Segment(segment18).
Segment(segment19).
Switch(switch6).
Segment(segment20).
Segment(segment21).
Sensor(sensor19).
Segment(segment23).
Sensor(sensor20).
Sensor(sensor21).
Sensor(sensor22).
Switch(switch7).
Sensor(sensor23).
Sensor(sensor25).
Segment(segment26).
Segment(segment27).
Sensor(sensor26).
Sensor(sensor27).
Sensor(sensor28).
Switch(switch8).
Segment(segment28).
Segment(segment29).
Sensor(sensor29).
Sensor(sensor30).
Sensor(sensor31).
Sensor(sensor32).
Sensor(sensor33).
Sensor(sensor34).
Sensor(sensor35).
sensors(region1, sensor4).
sensors(region1, sensor20).
sensors(region1, sensor28).
sensors(region1, sensor33).
sensors(region2, sensor3).
sensors(region2, sensor6).
sensors(region2, sensor11).
sensors(region2, sensor27).
sensors(region2, sensor29).
sensors(region3, sensor5).
sensors(region3, sensor15).
sensors(region4, sensor16).
sensors(region4, sensor23).
sensors(region4, sensor35).
sensors(region5, sensor14).
sensors(region5, sensor31).
sensors(region5, sensor32).
sensors(region5, sensor34).
sensors(region6, sensor9).
sensors(region6, sensor19).
sensors(region6, sensor21).
sensors(region6, sensor24).
sensors(region6, sensor26).
sensors(region7, sensor8).
sensors(region7, sensor10).
sensors(region7, sensor17).
sensors(region7, sensor18).
sensors(region7, sensor30).
sensors(region8, sensor7).
sensors(region8, sensor22).
sensors(region8, sensor25).
elements(region1, segment2).
elements(region1, segment3).
elements(region1, segment4).
elements(region1, switch2).
elements(region1, segment14).
elements(region1, segment15).
elements(region2, segment7).
elements(region2, switch1).
elements(region2, segment8).
elements(region3, segment1).
elements(region3, segment9).
elements(region3, segment11).
elements(region3, segment18).
elements(region3, segment19).
elements(region3, segment24).
elements(region4, segment27).
elements(region4, segment28).
elements(region5, segment6).
elements(region5, switch3).
elements(region5, segment10).
elements(region5, segment16).
elements(region5, segment21).
elements(region5, segment25).
elements(region5, segment29).
elements(region6, switch4).
elements(region6, segment12).
elements(region6, segment20).
elements(region6, segment23).
elements(region6, switch7).
elements(region6, segment26).
elements(region7, segment13).
elements(region7, switch5).
elements(region8, segment22).
elements(region8, switch8).
monitoredBy(segment1, sensor1).
monitoredBy(segment1, sensor3).
monitoredBy(segment1, sensor4).
monitoredBy(segment1, sensor7).
monitoredBy(segment1, sensor8).
monitoredBy(segment1, sensor15).
monitoredBy(segment1, sensor26).
monitoredBy(segment2, sensor4).
monitoredBy(segment2, sensor5).
monitoredBy(segment2, sensor10).
monitoredBy(segment2, sensor11).
monitoredBy(segment2, sensor14).
monitoredBy(segment2, sensor16).
monitoredBy(segment2, sensor25).
monitoredBy(segment3, sensor4).
monitoredBy(segment3, sensor6).
monitoredBy(segment3, sensor9).
monitoredBy(segment3, sensor13).
monitoredBy(segment3, sensor14).
monitoredBy(segment3, sensor16).
monitoredBy(segment3, sensor18).
monitoredBy(segment4, sensor5).
monitoredBy(segment4, sensor6).
monitoredBy(segment4, sensor9).
monitoredBy(segment4, sensor17).
monitoredBy(segment4, sensor20).
monitoredBy(segment4, sensor25).
monitoredBy(segment5, sensor4).
monitoredBy(segment5, sensor6).
monitoredBy(segment5, sensor12).
monitoredBy(segment5, sensor14).
monitoredBy(segment5, sensor21).
monitoredBy(segment6, sensor1).
monitoredBy(segment6, sensor5).
monitoredBy(segment6, sensor6).
monitoredBy(segment6, sensor9).
monitoredBy(segment6, sensor14).
monitoredBy(segment6, sensor20).
monitoredBy(segment7, sensor1).
monitoredBy(segment7, sensor2).
monitoredBy(segment7, sensor14).
monitoredBy(segment7, sensor15).
monitoredBy(segment7, sensor21).
monitoredBy(segment7, sensor22).
monitoredBy(switch1, sensor1).
monitoredBy(switch1, sensor2).
monitoredBy(switch1, sensor31).
monitoredBy(switch2, sensor3).
monitoredBy(switch2, sensor19).
monitoredBy(switch2, sensor22).
monitoredBy(segment8, sensor1).
monitoredBy(segment8, sensor2).
monitoredBy(segment8, sensor7).
monitoredBy(segment8, sensor8).
monitoredBy(segment8, sensor19).
monitoredBy(switch3, sensor5).
monitoredBy(switch3, sensor11).
monitoredBy(switch3, sensor16).
monitoredBy(switch3, sensor28).
monitoredBy(switch3, sensor30).
monitoredBy(segment9, sensor2).
monitoredBy(segment9, sensor11).
monitoredBy(segment9, sensor35).
monitoredBy(segment10, sensor4).
monitoredBy(segment10, sensor7).
monitoredBy(segment10, sensor8).
monitoredBy(segment10, sensor11).
monitoredBy(segment10, sensor21).
monitoredBy(switch4, sensor4).
monitoredBy(switch4, sensor8).
monitoredBy(switch4, sensor13).
monitoredBy(switch4, sensor14).
monitoredBy(switch4, sensor23).
monitoredBy(switch4, sensor24).
monitoredBy(segment11, sensor5).
monitoredBy(segment11, sensor11).
monitoredBy(segment11, sensor17).
monitoredBy(segment11, sensor22).
monitoredBy(segment11, sensor26).
monitoredBy(segment12, sensor1).
monitoredBy(segment12, sensor5).
monitoredBy(segment12, sensor9).
monitoredBy(segment12, sensor12).
monitoredBy(segment12, sensor27).
monitoredBy(segment12, sensor31).
monitoredBy(segment12, sensor33).
monitoredBy(segment13, sensor3).
monitoredBy(segment13, sensor4).
monitoredBy(segment13, sensor7).
monitoredBy(segment13, sensor12).
monitoredBy(segment13, sensor13).
monitoredBy(segment13, sensor16).
monitoredBy(segment13, sensor32).
monitoredBy(segment14, sensor1).
monitoredBy(segment14, sensor2).
monitoredBy(segment14, sensor11).
monitoredBy(segment14, sensor12).
monitoredBy(segment14, sensor21).
monitoredBy(segment15, sensor6).
monitoredBy(segment15, sensor7).
monitoredBy(segment15, sensor9).
monitoredBy(segment15, sensor15).
monitoredBy(segment15, sensor16).
monitoredBy(switch5, sensor15).
monitoredBy(switch5, sensor17).
monitoredBy(switch5, sensor33).
monitoredBy(segment16, sensor1).
monitoredBy(segment16, sensor5).
monitoredBy(segment16, sensor9).
monitoredBy(segment16, sensor12).
monitoredBy(segment16, sensor20).
monitoredBy(segment16, sensor25).
monitoredBy(segment17, sensor1).
monitoredBy(segment17, sensor2).
monitoredBy(segment17, sensor8).
monitoredBy(segment17, sensor11).
monitoredBy(segment17, sensor13).
monitoredBy(segment17, sensor21).
monitoredBy(segment17, sensor32).
monitoredBy(segment18, sensor3).
monitoredBy(segment18, sensor4).
monitoredBy(segment18, sensor5).
monitoredBy(segment18, sensor14).
monitoredBy(segment18, sensor16).
monitoredBy(segment18, sensor18).
monitoredBy(segment18, sensor21).
monitoredBy(segment19, sensor4).
monitoredBy(segment19, sensor6).
monitoredBy(segment19, sensor17).
monitoredBy(segment19, sensor24).
monitoredBy(segment19, sensor32).
monitoredBy(switch6, sensor9).
monitoredBy(switch6, sensor12).
monitoredBy(switch6, sensor20).
monitoredBy(segment20, sensor1).
monitoredBy(segment20, sensor2).
monitoredBy(segment20, sensor6).
monitoredBy(segment20, sensor7).
monitoredBy(segment20, sensor10).
monitoredBy(segment20, sensor19).
monitoredBy(segment20, sensor32).
monitoredBy(segment21, sensor2).
monitoredBy(segment21, sensor15).
monitoredBy(segment21, sensor16).
monitoredBy(segment21, sensor21).
monitoredBy(segment21, sensor29).
monitoredBy(segment22, sensor1).
monitoredBy(segment22, sensor2).
monitoredBy(segment22, sensor3).
monitoredBy(segment22, sensor5).
monitoredBy(segment22, sensor7).
monitoredBy(segment22, sensor8).
monitoredBy(segment22, sensor14).
monitoredBy(segment22, sensor19).
monitoredBy(segment23, sensor8).
monitoredBy(segment23, sensor11).
monitoredBy(segment23, sensor14).
monitoredBy(segment23, sensor23).
monitoredBy(segment24, sensor3).
monitoredBy(segment24, sensor5).
monitoredBy(segment24, sensor23).
monitoredBy(segment24, sensor25).
monitoredBy(segment25, sensor3).
monitoredBy(segment25, sensor14).
monitoredBy(segment25, sensor24).
monitoredBy(segment25, sensor28).
monitoredBy(switch7, sensor10).
monitoredBy(switch7, sensor21).
monitoredBy(segment26, sensor2).
monitoredBy(segment26, sensor5).
monitoredBy(segment26, sensor8).
monitoredBy(segment26, sensor11).
monitoredBy(segment26, sensor21).
monitoredBy(segment26, sensor25).
monitoredBy(segment26, sensor32).
monitoredBy(segment27, sensor9).
monitoredBy(segment27, sensor12).
monitoredBy(segment27, sensor16).
monitoredBy(segment27, sensor25).
monitoredBy(segment27, sensor27).
monitoredBy(segment27, sensor28).
monitoredBy(segment27, sensor34).
monitoredBy(switch8, sensor26).
monitoredBy(switch8, sensor27).
monitoredBy(segment28, sensor1).
monitoredBy(segment28, sensor3).
monitoredBy(segment29, sensor1).
monitoredBy(segment29, sensor6).
monitoredBy(segment29, sensor8).
monitoredBy(segment29, sensor13).
monitoredBy(segment29, sensor19).
monitoredBy(segment29, sensor31).
straight(switch1, segment6).
straight(switch2, segment6).
straight(switch3, segment4).
straight(switch4, segment22).
straight(switch5, segment6).
straight(switch6, segment2).
straight(switch7, segment14).
straight(switch8, segment14).
diverging(switch1, segment1).
diverging(switch2, segment9).
diverging(switch3, segment20).
diverging(switch4, switch3).
diverging(switch5, segment2).
diverging(switch6, segment6).
diverging(switch7, segment11).
connectsTo(segment1, switch3).
connectsTo(segment1, segment9).
connectsTo(segment2, switch2).
connectsTo(segment2, segment8).
connectsTo(segment3, switch1).
connectsTo(segment3, switch2).
connectsTo(segment4, segment7).
connectsTo(segment4, switch1).
connectsTo(segment5, switch1).
connectsTo(segment5, segment9).
connectsTo(segment6, switch2).
connectsTo(segment6, segment10).
connectsTo(segment7, segment4).
connectsTo(switch1, segment3).
connectsTo(switch1, segment4).
connectsTo(switch1, segment5).
connectsTo(switch2, segment2).
connectsTo(switch2, segment3).
connectsTo(switch2, segment6).
connectsTo(segment8, segment2).
connectsTo(segment8, switch3).
connectsTo(switch3, segment1).
connectsTo(switch3, segment8).
connectsTo(segment9, segment1).
connectsTo(segment9, segment5).
connectsTo(segment10, segment6).
connectsTo(segment10, segment12).
connectsTo(switch4, segment11).
connectsTo(switch4, segment12).
connectsTo(switch4, segment14).
connectsTo(segment11, switch4).
connectsTo(segment11, segment13).
connectsTo(segment12, segment10).
connectsTo(segment13, segment11).
connectsTo(segment13, switch5).
connectsTo(segment14, switch4).
connectsTo(segment14, segment15).
connectsTo(segment15, segment14).
connectsTo(segment15, segment17).
connectsTo(switch5, segment13).
connectsTo(switch5, segment16).
connectsTo(switch5, segment18).
connectsTo(segment16, switch5).
connectsTo(segment16, segment19).
connectsTo(segment17, segment15).
connectsTo(segment17, segment25).
connectsTo(segment18, switch5).
connectsTo(segment18, segment20).
connectsTo(segment19, segment16).
connectsTo(switch6, segment19).
connectsTo(switch6, segment21).
connectsTo(switch6, segment22).
connectsTo(segment20, segment18).
connectsTo(segment20, segment23).
connectsTo(segment21, switch6).
connectsTo(segment21, segment24).
connectsTo(segment22, switch6).
connectsTo(segment22, segment27).
connectsTo(segment23, segment20).
connectsTo(segment23, switch7).
connectsTo(segment24, segment21).
connectsTo(segment24, switch7).
connectsTo(segment25, segment26).
connectsTo(switch7, segment23).
connectsTo(switch7, segment24).
connectsTo(switch7, switch8).
connectsTo(segment26, segment25).
connectsTo(segment26, segment28).
connectsTo(segment27, segment22).
connectsTo(segment27, segment29).
connectsTo(switch8, switch7).
connectsTo(switch8, segment28).
connectsTo(segment28, segment26).
connectsTo(segment28, switch8).
connectsTo(segment29, segment27).

default !connectsTo(*, *).
?connectsTo(*, Segment::new).
?connectsTo(Segment::new, *).

default !monitoredBy(*, *).
?monitoredBy(Sensor::new, *).
