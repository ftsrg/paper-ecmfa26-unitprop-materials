
abstract class CompositeElement {
    contains Region[] regions
}

class Region {
    contains Vertex[] vertices opposite region
}

abstract class Vertex {
    container Region region opposite vertices
    contains Transition[] outgoingTransition opposite source
    Transition[] incomingTransition opposite target
}

class Transition {
    container Vertex source opposite outgoingTransition
    Vertex[1] target opposite incomingTransition
}

abstract class Pseudostate extends Vertex.

abstract class RegularState extends Vertex.

class Entry extends Pseudostate.

class Exit extends Pseudostate.

class Choice extends Pseudostate.

class FinalState extends RegularState.

class State extends RegularState, CompositeElement.

class Statechart extends CompositeElement.



pred entryInRegion(r, Entry e) <->
    vertices(r, e).

error noEntryInRegion(Region r) <->
    !entryInRegion(r, _).

error multipleEntryInRegion(r) <->
    Entry(e1),
    Entry(e2),
    vertices(r, e1),
    vertices(r, e2),
    e1 != e2.

error incomingToEntry(t, Entry e) <->
    target(t, e).

error noOutgoingTransitionFromEntry(Entry e) <->
    !source(_, e).

error multipleTransitionFromEntry(Entry e, t1, t2) <->
    outgoingTransition(e, t1),
    outgoingTransition(e, t2),
    t1 != t2.


error outgoingFromExit(t, Exit e) <->
    source(t, e).


error outgoingFromFinal(t, FinalState e) <->
    source(t, e).


pred stateInRegion(r, State s) <->
    vertices(r, s).

error noStateInRegion(Region r) <->
    !stateInRegion(r, _).


error choiceHasNoOutgoing(Choice c) <->
    !source(_, c).

error choiceHasNoIncoming(Choice c) <->
    !target(_, c).

declare sct, transition1, region1, region2, exit1, exit2, choice1, state1,
	transition2, state2, region3, entry1, entry2, finalState1, state3,
	finalState2, state4, transition3, transition4, finalState3, state5, exit3,
	exit4, transition5, exit5, finalState4, state6, exit6, choice2,
	finalState5, exit7, transition6, transition7, transition8, entry3,
	transition9.
Transition(transition1).
Statechart(sct).
Region(region1).
Region(region2).
Exit(exit1).
Exit(exit2).
Choice(choice1).
State(state1).
Transition(transition2).
State(state2).
Entry(entry1).
FinalState(finalState1).
State(state3).
FinalState(finalState2).
State(state4).
Transition(transition3).
Transition(transition4).
FinalState(finalState3).
State(state5).
Exit(exit3).
Exit(exit4).
Transition(transition5).
Exit(exit5).
FinalState(finalState4).
State(state6).
Exit(exit6).
Choice(choice2).
FinalState(finalState5).
Exit(exit7).
Transition(transition6).
Transition(transition7).
Transition(transition8).
Entry(entry3).
Transition(transition9).
regions(sct, region1).
regions(sct, region2).
vertices(region1, choice1).
vertices(region1, finalState2).
vertices(region1, finalState3).
vertices(region1, state5).
vertices(region1, exit3).
vertices(region1, entry3).
vertices(region2, exit1).
vertices(region2, exit2).
vertices(region2, state2).
vertices(region2, state4).
vertices(region2, exit4).
vertices(region2, exit5).
vertices(region2, finalState4).
vertices(region2, state6).
vertices(region2, exit6).
vertices(region2, choice2).
vertices(region2, finalState5).
vertices(region3, entry1).
vertices(region3, state3).
vertices(region3, exit7).
outgoingTransition(choice1, transition8).
outgoingTransition(state1, transition2).
outgoingTransition(state2, transition4).
outgoingTransition(entry1, transition9).
outgoingTransition(state3, transition5).
outgoingTransition(state4, transition3).
outgoingTransition(choice2, transition7).
outgoingTransition(entry3, transition1).
incomingTransition(exit1, transition1).
incomingTransition(exit1, transition4).
incomingTransition(exit2, transition9).
incomingTransition(choice1, transition7).
incomingTransition(finalState3, transition3).
incomingTransition(exit4, transition5).
incomingTransition(choice2, transition8).
incomingTransition(exit7, transition6).
