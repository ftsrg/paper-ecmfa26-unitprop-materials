import builtin::view.

@color(hex="#ffffff")
abstract class TrackElement {
    TrackElement[1..3] connectsTo opposite connectsTo
    Sensor[] monitoredBy opposite monitors
}

@color(hex="#218380")
class Segment extends TrackElement {
}

@color(hex="#8f2d56")
class Switch extends TrackElement {
    TrackElement straight
    TrackElement diverging
}
class Sensor {
    TrackElement[] monitors opposite monitoredBy
}
class Region {
    contains Sensor[] sensors
    contains TrackElement[] elements
}


error sameStraightDiverging(switch, target) <->
    straight(switch,target),
    diverging(switch,target).

error noDiverging(switch) <->
    Switch(switch),
    !diverging(switch,_).

error noStraight(switch) <->
    Switch(switch),
    !straight(switch,_).

error tooManyConncetions(segment) <->
    Segment(segment),
    count{connectsTo(segment, _)} > 2.


error selfLoop(e) <->
    TrackElement(e),
    connectsTo(e, e)
;
    Switch(e),
    straight(e, e)
;
    Switch(e),
    diverging(e, e).


error underconnectedSwitch(sw) <->
    Switch(sw),
    !switchWithTwoConnections(sw, _, _).

pred switchWithTwoConnections(sw, te1, te2) <->
    Switch(sw),
    connectsTo(sw, te1),
    connectsTo(sw, te2),
    te1 != te2.


error sensorWithoutElement(sensor) <->
    Sensor(sensor),
    !monitors(sensor, _).

error sensorMonitorsMultipleSwitches(sensor, sw1, sw2) <->
    Sensor(sensor),
    monitors(sensor, sw1),
    monitors(sensor, sw2),
    Switch(sw1),
    Switch(sw2),
    sw1 != sw2.

error duplicateMonitors(region, sensor1, sensor2, te) <->
    Region(region),
    sensors(region, sensor1),
    sensors(region, sensor2),
    sensor1 != sensor2,
    monitors(sensor1, te),
    monitors(sensor2, te).

error emptyRegion(region) <->
    Region(region),
    !elements(region, _).


error connectedSegments(sensor, segment1, segment2, segment3, segment4, segment5, segment6) <->
    connectsTo(segment1, segment2),
    connectsTo(segment2, segment3),
    connectsTo(segment3, segment4),
    connectsTo(segment4, segment5),
    connectsTo(segment5, segment6),
    monitoredBy(segment1, sensor),
    monitoredBy(segment2, sensor),
    monitoredBy(segment3, sensor),
    monitoredBy(segment4, sensor),
    monitoredBy(segment5, sensor),
    monitoredBy(segment6, sensor).


error switchMonitored(sw) <-> 
    Switch(sw),
    !monitoredBy(sw, _).

declare region1, segment1, region2, sensor1, sensor2, region3, sensor3,
	sensor4, segment2, segment3, sensor5, sensor6, sensor7, sensor8, sensor9,
	sensor10, sensor11, segment4, sensor12, sensor13, sensor14, segment5,
	sensor15, segment6, segment7, switch1, segment8, switch2, switch3,
	segment9, switch4.
Region(region1).
Region(region2).
Sensor(sensor1).
Sensor(sensor2).
Sensor(sensor3).
Sensor(sensor4).
Segment(segment2).
Segment(segment3).
Sensor(sensor5).
Sensor(sensor6).
Sensor(sensor8).
Sensor(sensor9).
Sensor(sensor10).
Segment(segment4).
Sensor(sensor12).
Sensor(sensor13).
Sensor(sensor14).
Segment(segment5).
Sensor(sensor15).
Segment(segment6).
Segment(segment7).
Switch(switch1).
Segment(segment8).
Switch(switch2).
Switch(switch4).
sensors(region1, sensor2).
sensors(region1, sensor9).
sensors(region1, sensor11).
sensors(region2, sensor1).
sensors(region2, sensor5).
sensors(region2, sensor8).
sensors(region2, sensor12).
sensors(region2, sensor13).
sensors(region2, sensor15).
sensors(region3, sensor3).
sensors(region3, sensor4).
sensors(region3, sensor6).
sensors(region3, sensor14).
elements(region1, switch3).
elements(region1, segment9).
elements(region2, segment5).
elements(region2, segment6).
elements(region2, segment7).
elements(region2, segment8).
elements(region2, switch2).
elements(region2, switch4).
elements(region3, segment2).
elements(region3, segment3).
elements(region3, segment4).
elements(region3, switch1).
monitoredBy(segment1, sensor1).
monitoredBy(segment1, sensor2).
monitoredBy(segment1, sensor3).
monitoredBy(segment2, sensor4).
monitoredBy(segment2, sensor7).
monitoredBy(segment2, sensor8).
monitoredBy(segment3, sensor6).
monitoredBy(segment3, sensor9).
monitoredBy(segment4, sensor8).
monitoredBy(segment4, sensor11).
monitoredBy(segment4, sensor14).
monitoredBy(segment5, sensor9).
monitoredBy(segment5, sensor10).
monitoredBy(segment5, sensor14).
monitoredBy(segment6, sensor2).
monitoredBy(segment6, sensor4).
monitoredBy(segment6, sensor15).
monitoredBy(segment7, sensor7).
monitoredBy(segment7, sensor14).
monitoredBy(switch1, sensor4).
monitoredBy(switch1, sensor7).
monitoredBy(switch1, sensor15).
monitoredBy(segment8, sensor6).
monitoredBy(segment8, sensor11).
monitoredBy(switch2, sensor2).
monitoredBy(switch2, sensor6).
monitoredBy(switch3, sensor10).
monitoredBy(switch3, sensor11).
monitoredBy(segment9, sensor11).
monitoredBy(segment9, sensor13).
monitoredBy(segment9, sensor14).
monitoredBy(switch4, sensor1).
monitoredBy(switch4, sensor9).
straight(switch1, segment4).
straight(switch2, segment4).
straight(switch3, segment1).
diverging(switch1, segment1).
diverging(switch2, segment3).
diverging(switch3, segment5).
diverging(switch4, segment6).
connectsTo(segment1, switch1).
connectsTo(segment2, switch4).
connectsTo(segment3, switch3).
connectsTo(segment4, switch1).
connectsTo(segment5, switch2).
connectsTo(segment6, switch3).
connectsTo(segment6, switch4).
connectsTo(segment7, switch2).
connectsTo(segment7, switch3).
connectsTo(switch1, segment1).
connectsTo(switch1, segment3).
connectsTo(switch1, segment4).
connectsTo(segment8, switch4).
connectsTo(switch2, segment5).
connectsTo(switch2, segment7).
connectsTo(switch2, segment9).
connectsTo(switch3, segment3).
connectsTo(switch3, segment6).
connectsTo(switch3, segment7).
connectsTo(segment9, switch2).
connectsTo(switch4, segment2).
connectsTo(switch4, segment6).
connectsTo(switch4, segment8).

default !connectsTo(*, *).
?connectsTo(*, Segment::new).
?connectsTo(Segment::new, *).

default !monitoredBy(*, *).
?monitoredBy(*, Segment::new).
?monitoredBy(Sensor::new, *).
