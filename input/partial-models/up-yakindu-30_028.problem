
abstract class CompositeElement {
    contains Region[] regions
}

class Region {
    contains Vertex[] vertices opposite region
}

abstract class Vertex {
    container Region region opposite vertices
    contains Transition[] outgoingTransition opposite source
    Transition[] incomingTransition opposite target
}

class Transition {
    container Vertex source opposite outgoingTransition
    Vertex[1] target opposite incomingTransition
}

abstract class Pseudostate extends Vertex.

abstract class RegularState extends Vertex.

class Entry extends Pseudostate.

class Exit extends Pseudostate.

class Choice extends Pseudostate.

class FinalState extends RegularState.

class State extends RegularState, CompositeElement.

class Statechart extends CompositeElement.



pred entryInRegion(r, Entry e) <->
    vertices(r, e).

error noEntryInRegion(Region r) <->
    !entryInRegion(r, _).

error multipleEntryInRegion(r) <->
    Entry(e1),
    Entry(e2),
    vertices(r, e1),
    vertices(r, e2),
    e1 != e2.

error incomingToEntry(t, Entry e) <->
    target(t, e).

error noOutgoingTransitionFromEntry(Entry e) <->
    !source(_, e).

error multipleTransitionFromEntry(Entry e, t1, t2) <->
    outgoingTransition(e, t1),
    outgoingTransition(e, t2),
    t1 != t2.


error outgoingFromExit(t, Exit e) <->
    source(t, e).


error outgoingFromFinal(t, FinalState e) <->
    source(t, e).


pred stateInRegion(r, State s) <->
    vertices(r, s).

error noStateInRegion(Region r) <->
    !stateInRegion(r, _).


error choiceHasNoOutgoing(Choice c) <->
    !source(_, c).

error choiceHasNoIncoming(Choice c) <->
    !target(_, c).

declare sct, transition1, region1, entry1, exit1, region2, region3, entry2,
	transition2, transition3, exit2, exit3, state1, exit4, exit5, state2,
	exit6, state3, finalState1, entry3, choice1, transition4, state4,
	finalState2, finalState3, transition5, transition6, state5, finalState4,
	exit7, state6, exit8, choice2, transition7, transition8, transition9.
Transition(transition1).
Statechart(sct).
Region(region1).
Entry(entry1).
Exit(exit1).
Region(region2).
Region(region3).
Entry(entry2).
Transition(transition2).
Transition(transition3).
Exit(exit2).
Exit(exit3).
State(state1).
Exit(exit4).
Exit(exit5).
State(state2).
Exit(exit6).
State(state3).
FinalState(finalState1).
Choice(choice1).
Transition(transition4).
State(state4).
FinalState(finalState2).
Transition(transition5).
Transition(transition6).
State(state5).
FinalState(finalState4).
Exit(exit7).
State(state6).
Exit(exit8).
Transition(transition7).
Transition(transition9).
regions(sct, region1).
regions(sct, region2).
regions(sct, region3).
vertices(region1, entry1).
vertices(region1, exit1).
vertices(region1, state1).
vertices(region1, exit6).
vertices(region1, state3).
vertices(region1, finalState2).
vertices(region1, choice2).
vertices(region2, entry2).
vertices(region2, exit2).
vertices(region2, exit4).
vertices(region2, exit5).
vertices(region2, finalState1).
vertices(region2, choice1).
vertices(region2, state4).
vertices(region2, state5).
vertices(region3, exit3).
vertices(region3, state2).
vertices(region3, entry3).
vertices(region3, finalState3).
vertices(region3, finalState4).
vertices(region3, exit7).
vertices(region3, exit8).
outgoingTransition(entry1, transition3).
outgoingTransition(entry2, transition2).
outgoingTransition(state1, transition5).
outgoingTransition(entry3, transition4).
outgoingTransition(choice1, transition7).
outgoingTransition(state4, transition6).
outgoingTransition(state6, transition1).
outgoingTransition(choice2, transition8).
outgoingTransition(choice2, transition9).
incomingTransition(exit1, transition4).
incomingTransition(exit3, transition3).
incomingTransition(exit3, transition7).
incomingTransition(exit4, transition5).
incomingTransition(state2, transition8).
incomingTransition(choice1, transition9).
incomingTransition(finalState2, transition6).
incomingTransition(choice2, transition1).
