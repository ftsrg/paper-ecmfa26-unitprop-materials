import builtin::view.

@color(hex="#ffffff")
abstract class TrackElement {
    TrackElement[1..3] connectsTo opposite connectsTo
}

@color(hex="#218380")
class Segment extends TrackElement {
}

@color(hex="#8f2d56")
class Switch extends TrackElement {
    TrackElement straight
    TrackElement diverging
}


error sameStraightDiverging(switch, target) <->
    straight(switch,target),
    diverging(switch,target).

error noDiverging(switch) <->
    Switch(switch),
    !diverging(switch,_).

error noStraight(switch) <->
    Switch(switch),
    !straight(switch,_).

error tooManyConncetions(segment) <->
    Segment(segment),
    count{connectsTo(segment, _)} > 2.


error selfLoop(e) <->
    TrackElement(e),
    connectsTo(e, e)
;
    Switch(e),
    straight(e, e)
;
    Switch(e),
    diverging(e, e).
    

error underconnectedSwitch(sw) <->
    Switch(sw),
    !switchWithTwoConnections(sw, _, _).

pred switchWithTwoConnections(sw, te1, te2) <->
    Switch(sw),
    connectsTo(sw, te1),
    connectsTo(sw, te2),
    te1 != te2.

declare segment1, segment2, segment3, segment4, switch1, segment5, segment6,
	segment7, segment8, switch2, segment9, segment10, segment11, segment12,
	segment13, switch3, switch4, segment14, switch5, switch6.
declare segment15, segment16, segment17, segment18, segment19.
declare segment20, switch7, segment21, segment22, switch8.
Switch(segment1).
Segment(segment2).
Segment(segment3).
Segment(segment4).
Switch(switch1).
Segment(segment5).
Segment(segment6).
Switch(segment7).
Segment(segment8).
Segment(segment9).
Segment(segment10).
Segment(segment11).
Segment(segment12).
Segment(segment13).
Switch(switch3).
Switch(switch4).
Segment(segment14).
Switch(switch5).
Switch(switch6).
Segment(segment15).
Segment(segment16).
Segment(segment17).
Segment(segment18).
Segment(segment20).
Switch(switch7).
Segment(segment21).
Segment(segment22).
Switch(switch8).
straight(switch1, segment1).
straight(switch2, segment4).
straight(switch3, segment12).
straight(switch5, segment8).
straight(switch6, segment13).
straight(switch7, segment2).
straight(switch8, segment14).
diverging(segment1, switch6).
diverging(switch1, segment4).
diverging(segment7, segment10).
diverging(switch2, segment3).
diverging(switch6, segment14).
diverging(switch7, segment20).
diverging(switch8, segment15).
connectsTo(segment1, switch1).
connectsTo(segment1, segment12).
connectsTo(segment1, segment15).
connectsTo(segment2, switch1).
connectsTo(segment2, switch3).
connectsTo(segment3, switch2).
connectsTo(segment3, switch4).
connectsTo(segment4, segment16).
connectsTo(segment4, segment17).
connectsTo(switch1, segment1).
connectsTo(switch1, segment2).
connectsTo(switch1, segment6).
connectsTo(segment5, segment6).
connectsTo(segment5, switch5).
connectsTo(segment6, switch1).
connectsTo(segment6, segment5).
connectsTo(segment7, switch2).
connectsTo(segment7, segment17).
connectsTo(segment8, switch2).
connectsTo(segment8, segment9).
connectsTo(switch2, segment3).
connectsTo(switch2, segment7).
connectsTo(switch2, segment8).
connectsTo(segment9, segment8).
connectsTo(segment10, switch3).
connectsTo(segment10, switch5).
connectsTo(segment11, switch6).
connectsTo(segment11, segment15).
connectsTo(segment12, segment1).
connectsTo(segment12, switch6).
connectsTo(segment13, segment9).
connectsTo(segment13, switch4).
connectsTo(switch3, segment2).
connectsTo(switch3, segment10).
connectsTo(switch3, segment19).
connectsTo(switch4, segment3).
connectsTo(switch4, segment13).
connectsTo(switch4, segment18).
connectsTo(segment14, switch5).
connectsTo(switch5, segment5).
connectsTo(switch5, segment10).
connectsTo(switch5, segment14).
connectsTo(switch6, segment12).
connectsTo(switch6, segment14).
connectsTo(segment15, segment1).
connectsTo(segment15, segment11).
connectsTo(segment16, segment4).
connectsTo(segment16, segment18).
connectsTo(segment17, segment4).
connectsTo(segment17, segment7).
connectsTo(segment18, switch4).
connectsTo(segment18, segment16).
connectsTo(segment19, segment7).
connectsTo(segment19, switch3).
connectsTo(segment20, switch7).
connectsTo(segment20, switch8).
connectsTo(switch7, segment20).
connectsTo(switch7, segment21).
connectsTo(switch7, segment22).
connectsTo(segment21, switch7).
connectsTo(segment21, switch8).
connectsTo(segment22, switch7).
connectsTo(switch8, segment20).
connectsTo(switch8, segment21).
