
abstract class CompositeElement {
    contains Region[] regions
}

class Region {
    contains Vertex[] vertices opposite region
}

abstract class Vertex {
    container Region region opposite vertices
    contains Transition[] outgoingTransition opposite source
    Transition[] incomingTransition opposite target
}

class Transition {
    container Vertex source opposite outgoingTransition
    Vertex[1] target opposite incomingTransition
}

abstract class Pseudostate extends Vertex.

abstract class RegularState extends Vertex.

class Entry extends Pseudostate.

class Exit extends Pseudostate.

class Choice extends Pseudostate.

class FinalState extends RegularState.

class State extends RegularState, CompositeElement.

class Statechart extends CompositeElement.



pred entryInRegion(r, Entry e) <->
    vertices(r, e).

error noEntryInRegion(Region r) <->
    !entryInRegion(r, _).

error multipleEntryInRegion(r) <->
    Entry(e1),
    Entry(e2),
    vertices(r, e1),
    vertices(r, e2),
    e1 != e2.

error incomingToEntry(t, Entry e) <->
    target(t, e).

error noOutgoingTransitionFromEntry(Entry e) <->
    !source(_, e).

error multipleTransitionFromEntry(Entry e, t1, t2) <->
    outgoingTransition(e, t1),
    outgoingTransition(e, t2),
    t1 != t2.


error outgoingFromExit(t, Exit e) <->
    source(t, e).


error outgoingFromFinal(t, FinalState e) <->
    source(t, e).


pred stateInRegion(r, State s) <->
    vertices(r, s).

error noStateInRegion(Region r) <->
    !stateInRegion(r, _).


error choiceHasNoOutgoing(Choice c) <->
    !source(_, c).

error choiceHasNoIncoming(Choice c) <->
    !target(_, c).

declare sct, transition1, region1, choice1, finalState1, finalState2, exit1,
	entry1, transition2, choice2, finalState3, transition3, transition4,
	exit2, exit3, state1, transition5, transition6, state2, transition7,
	exit4, region2, finalState4, state3, transition8, transition9, exit5,
	transition10, finalState5, state4, region3, transition11, state5, entry2,
	transition12, entry3.
Transition(transition1).
Statechart(sct).
Region(region1).
Choice(choice1).
FinalState(finalState1).
FinalState(finalState2).
Exit(exit1).
Entry(entry1).
Transition(transition2).
Choice(choice2).
FinalState(finalState3).
Transition(transition3).
Exit(exit2).
Exit(exit3).
State(state1).
Transition(transition5).
Transition(transition6).
State(state2).
Transition(transition7).
Exit(exit4).
FinalState(finalState4).
Transition(transition8).
Transition(transition9).
Exit(exit5).
Transition(transition10).
FinalState(finalState5).
State(state4).
Region(region3).
Transition(transition11).
State(state5).
Transition(transition12).
Entry(entry3).
regions(sct, region1).
regions(sct, region3).
vertices(region1, choice1).
vertices(region1, finalState1).
vertices(region1, finalState2).
vertices(region1, exit1).
vertices(region1, entry1).
vertices(region1, choice2).
vertices(region1, finalState3).
vertices(region1, exit2).
vertices(region1, exit3).
vertices(region1, state1).
vertices(region1, state2).
vertices(region1, exit4).
vertices(region1, state4).
vertices(region2, finalState4).
vertices(region2, state3).
vertices(region2, exit5).
vertices(region2, finalState5).
vertices(region2, entry2).
vertices(region3, state5).
vertices(region3, entry3).
outgoingTransition(choice1, transition2).
outgoingTransition(choice1, transition4).
outgoingTransition(choice1, transition5).
outgoingTransition(choice1, transition10).
outgoingTransition(entry1, transition11).
outgoingTransition(choice2, transition3).
outgoingTransition(choice2, transition6).
outgoingTransition(state1, transition7).
outgoingTransition(state1, transition8).
outgoingTransition(state1, transition9).
outgoingTransition(entry2, transition12).
outgoingTransition(entry3, transition1).
incomingTransition(choice1, transition1).
incomingTransition(finalState1, transition4).
incomingTransition(finalState2, transition9).
incomingTransition(exit1, transition2).
incomingTransition(choice2, transition3).
incomingTransition(exit2, transition5).
incomingTransition(exit3, transition11).
incomingTransition(state1, transition12).
incomingTransition(state2, transition10).
incomingTransition(exit4, transition7).
