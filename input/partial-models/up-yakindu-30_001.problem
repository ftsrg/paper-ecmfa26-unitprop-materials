
abstract class CompositeElement {
    contains Region[] regions
}

class Region {
    contains Vertex[] vertices opposite region
}

abstract class Vertex {
    container Region region opposite vertices
    contains Transition[] outgoingTransition opposite source
    Transition[] incomingTransition opposite target
}

class Transition {
    container Vertex source opposite outgoingTransition
    Vertex[1] target opposite incomingTransition
}

abstract class Pseudostate extends Vertex.

abstract class RegularState extends Vertex.

class Entry extends Pseudostate.

class Exit extends Pseudostate.

class Choice extends Pseudostate.

class FinalState extends RegularState.

class State extends RegularState, CompositeElement.

class Statechart extends CompositeElement.



pred entryInRegion(r, Entry e) <->
    vertices(r, e).

error noEntryInRegion(Region r) <->
    !entryInRegion(r, _).

error multipleEntryInRegion(r) <->
    Entry(e1),
    Entry(e2),
    vertices(r, e1),
    vertices(r, e2),
    e1 != e2.

error incomingToEntry(t, Entry e) <->
    target(t, e).

error noOutgoingTransitionFromEntry(Entry e) <->
    !source(_, e).

error multipleTransitionFromEntry(Entry e, t1, t2) <->
    outgoingTransition(e, t1),
    outgoingTransition(e, t2),
    t1 != t2.


error outgoingFromExit(t, Exit e) <->
    source(t, e).


error outgoingFromFinal(t, FinalState e) <->
    source(t, e).


pred stateInRegion(r, State s) <->
    vertices(r, s).

error noStateInRegion(Region r) <->
    !stateInRegion(r, _).


error choiceHasNoOutgoing(Choice c) <->
    !source(_, c).

error choiceHasNoIncoming(Choice c) <->
    !target(_, c).

declare sct, transition1, region1, state1, finalState1, finalState2,
	transition2, choice1, transition3, finalState3, entry1, region2, exit1,
	transition4, transition5, state2, finalState4, transition6, transition7,
	region3, state3, transition8, state4, choice2, finalState5, entry2,
	finalState6, transition9, transition10, state5, transition11, finalState7,
	transition12, state6, transition13, entry3.
Transition(transition1).
Statechart(sct).
Region(region1).
State(state1).
FinalState(finalState1).
FinalState(finalState2).
Transition(transition2).
Choice(choice1).
Transition(transition3).
FinalState(finalState3).
Region(region2).
Exit(exit1).
Transition(transition4).
State(state2).
FinalState(finalState4).
Transition(transition6).
Transition(transition7).
Region(region3).
State(state3).
Transition(transition8).
Choice(choice2).
FinalState(finalState5).
Entry(entry2).
FinalState(finalState6).
Transition(transition9).
Transition(transition10).
State(state5).
Transition(transition11).
FinalState(finalState7).
Transition(transition12).
State(state6).
Transition(transition13).
Entry(entry3).
regions(sct, region1).
regions(sct, region3).
regions(state1, region2).
vertices(region1, finalState1).
vertices(region1, finalState2).
vertices(region1, choice1).
vertices(region1, finalState3).
vertices(region1, entry1).
vertices(region1, exit1).
vertices(region1, state2).
vertices(region1, finalState4).
vertices(region1, state3).
vertices(region1, state4).
vertices(region1, finalState5).
vertices(region1, finalState6).
vertices(region1, finalState7).
vertices(region2, entry2).
vertices(region2, state6).
vertices(region3, state5).
vertices(region3, entry3).
outgoingTransition(state1, transition2).
outgoingTransition(state1, transition3).
outgoingTransition(state1, transition5).
outgoingTransition(state1, transition9).
outgoingTransition(state1, transition11).
outgoingTransition(choice1, transition7).
outgoingTransition(entry1, transition4).
outgoingTransition(state2, transition6).
outgoingTransition(choice2, transition13).
outgoingTransition(entry2, transition12).
outgoingTransition(entry3, transition1).
incomingTransition(finalState1, transition2).
incomingTransition(finalState1, transition8).
incomingTransition(finalState2, transition3).
incomingTransition(choice1, transition4).
incomingTransition(finalState3, transition5).
incomingTransition(finalState3, transition11).
incomingTransition(state2, transition6).
incomingTransition(finalState4, transition1).
incomingTransition(state3, transition12).
incomingTransition(state4, transition10).
incomingTransition(choice2, transition9).
incomingTransition(state5, transition13).
