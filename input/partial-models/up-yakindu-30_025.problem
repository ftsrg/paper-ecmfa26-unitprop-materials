
abstract class CompositeElement {
    contains Region[] regions
}

class Region {
    contains Vertex[] vertices opposite region
}

abstract class Vertex {
    container Region region opposite vertices
    contains Transition[] outgoingTransition opposite source
    Transition[] incomingTransition opposite target
}

class Transition {
    container Vertex source opposite outgoingTransition
    Vertex[1] target opposite incomingTransition
}

abstract class Pseudostate extends Vertex.

abstract class RegularState extends Vertex.

class Entry extends Pseudostate.

class Exit extends Pseudostate.

class Choice extends Pseudostate.

class FinalState extends RegularState.

class State extends RegularState, CompositeElement.

class Statechart extends CompositeElement.



pred entryInRegion(r, Entry e) <->
    vertices(r, e).

error noEntryInRegion(Region r) <->
    !entryInRegion(r, _).

error multipleEntryInRegion(r) <->
    Entry(e1),
    Entry(e2),
    vertices(r, e1),
    vertices(r, e2),
    e1 != e2.

error incomingToEntry(t, Entry e) <->
    target(t, e).

error noOutgoingTransitionFromEntry(Entry e) <->
    !source(_, e).

error multipleTransitionFromEntry(Entry e, t1, t2) <->
    outgoingTransition(e, t1),
    outgoingTransition(e, t2),
    t1 != t2.


error outgoingFromExit(t, Exit e) <->
    source(t, e).


error outgoingFromFinal(t, FinalState e) <->
    source(t, e).


pred stateInRegion(r, State s) <->
    vertices(r, s).

error noStateInRegion(Region r) <->
    !stateInRegion(r, _).


error choiceHasNoOutgoing(Choice c) <->
    !source(_, c).

error choiceHasNoIncoming(Choice c) <->
    !target(_, c).

declare sct, transition1, region1, finalState1, region2, finalState2, exit1,
	choice1, transition2, state1, state2, choice2, state3, exit2, finalState3,
	state4, transition3, state5, entry1, state6, transition4, transition5,
	state7, exit3, region3, transition6, entry2, exit4, transition7, exit5,
	transition8, entry3, transition9, transition10, transition11, state8.
Transition(transition1).
Statechart(sct).
Region(region1).
FinalState(finalState1).
Region(region2).
FinalState(finalState2).
Exit(exit1).
Choice(choice1).
Transition(transition2).
State(state1).
State(state2).
Exit(exit2).
FinalState(finalState3).
State(state4).
Transition(transition3).
State(state5).
Entry(entry1).
Transition(transition4).
Transition(transition5).
State(state7).
Exit(exit3).
Region(region3).
Transition(transition6).
Entry(entry2).
Exit(exit4).
Transition(transition7).
Exit(exit5).
Transition(transition8).
Entry(entry3).
Transition(transition9).
State(state8).
regions(sct, region1).
regions(sct, region2).
regions(state5, region3).
vertices(region1, finalState1).
vertices(region1, exit1).
vertices(region1, state1).
vertices(region1, state3).
vertices(region1, state5).
vertices(region1, entry1).
vertices(region1, state7).
vertices(region1, exit3).
vertices(region2, finalState2).
vertices(region2, choice1).
vertices(region2, choice2).
vertices(region2, state4).
vertices(region2, state6).
vertices(region2, entry3).
vertices(region3, entry2).
vertices(region3, exit4).
vertices(region3, exit5).
vertices(region3, state8).
outgoingTransition(choice1, transition2).
outgoingTransition(state1, transition4).
outgoingTransition(state2, transition1).
outgoingTransition(state2, transition5).
outgoingTransition(choice2, transition3).
outgoingTransition(state3, transition6).
outgoingTransition(state5, transition7).
outgoingTransition(state5, transition8).
outgoingTransition(entry2, transition11).
outgoingTransition(entry3, transition10).
incomingTransition(exit1, transition2).
incomingTransition(choice1, transition4).
incomingTransition(state2, transition5).
incomingTransition(choice2, transition1).
incomingTransition(state3, transition7).
incomingTransition(state3, transition8).
incomingTransition(state6, transition10).
incomingTransition(exit3, transition6).
incomingTransition(exit3, transition9).
incomingTransition(exit3, transition11).
