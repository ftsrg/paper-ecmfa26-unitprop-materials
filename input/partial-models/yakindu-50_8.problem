
abstract class CompositeElement {
    contains Region[] regions
}

class Region {
    contains Vertex[] vertices opposite region
}

abstract class Vertex {
    container Region region opposite vertices
    contains Transition[] outgoingTransition opposite source
    Transition[] incomingTransition opposite target
}

class Transition {
    container Vertex source opposite outgoingTransition
    Vertex[1] target opposite incomingTransition
}

abstract class Pseudostate extends Vertex.

abstract class RegularState extends Vertex.

class Entry extends Pseudostate.

class Exit extends Pseudostate.

class Choice extends Pseudostate.

class FinalState extends RegularState.

class State extends RegularState, CompositeElement.

class Statechart extends CompositeElement.



pred entryInRegion(r, Entry e) <->
    vertices(r, e).

error noEntryInRegion(Region r) <->
    !entryInRegion(r, _).

error multipleEntryInRegion(r) <->
    Entry(e1),
    Entry(e2),
    vertices(r, e1),
    vertices(r, e2),
    e1 != e2.

error incomingToEntry(t, Entry e) <->
    target(t, e).

error noOutgoingTransitionFromEntry(Entry e) <->
    !source(_, e).

error multipleTransitionFromEntry(Entry e, t1, t2) <->
    outgoingTransition(e, t1),
    outgoingTransition(e, t2),
    t1 != t2.


error outgoingFromExit(t, Exit e) <->
    source(t, e).


error outgoingFromFinal(t, FinalState e) <->
    source(t, e).


pred stateInRegion(r, State s) <->
    vertices(r, s).

error noStateInRegion(Region r) <->
    !stateInRegion(r, _).


error choiceHasNoOutgoing(Choice c) <->
    !source(_, c).

error choiceHasNoIncoming(Choice c) <->
    !target(_, c).

declare sct, transition1, region1, choice1, finalState1, exit1, entry1,
	region2, transition2, transition3, state1, state2, finalState2, state3,
	transition4, transition5, transition6, exit2, exit3, finalState3,
	finalState4, state4, entry2, state5.
declare transition7, region3, transition8, state6, exit4, entry3.
declare state7, state8.
declare transition9, transition10, exit5, state9, exit6, transition11,
	finalState5, finalState6, region4, state10.
declare finalState7, finalState8, finalState9, exit7, exit8, transition12.
declare transition13, exit9, transition14, exit10, state11, entry4.
declare exit11, state12, exit12, finalState10, finalState11, finalState12.
Exit(exit11).
Statechart(sct).
Transition(transition1).
Region(region1).
Choice(choice1).
State(finalState1).
State(exit1).
Entry(entry1).
Region(region2).
Transition(transition2).
Transition(transition3).
State(state1).
State(state2).
FinalState(finalState2).
Choice(state3).
Transition(transition4).
Transition(transition5).
Transition(transition6).
State(exit2).
FinalState(exit3).
Choice(finalState3).
Exit(finalState4).
Entry(state4).
Entry(entry2).
FinalState(state5).
Transition(transition7).
Region(region3).
Transition(transition8).
State(state6).
Exit(exit4).
Entry(entry3).
Transition(transition9).
Transition(transition10).
Exit(exit5).
State(state9).
Exit(exit6).
Transition(transition11).
FinalState(finalState5).
FinalState(finalState6).
Region(region4).
State(state10).
FinalState(finalState8).
FinalState(finalState9).
Exit(exit7).
Exit(exit8).
Transition(transition12).
Transition(transition13).
Exit(exit9).
Transition(transition14).
Exit(exit10).
State(state11).
State(state12).
Exit(exit12).
FinalState(finalState10).
FinalState(finalState11).
regions(sct, region4).
regions(state2, region3).
regions(exit2, region2).
regions(state8, state7).
regions(state10, region1).
vertices(region1, choice1).
vertices(region1, finalState1).
vertices(region1, exit1).
vertices(region1, entry1).
vertices(region1, state1).
vertices(region1, exit2).
vertices(region1, state5).
vertices(region1, state9).
vertices(region1, finalState5).
vertices(region1, finalState8).
vertices(region1, finalState9).
vertices(region1, exit8).
vertices(region2, exit3).
vertices(region2, finalState4).
vertices(region2, entry2).
vertices(region2, exit6).
vertices(region2, exit10).
vertices(region2, state12).
vertices(region3, state3).
vertices(region3, entry3).
vertices(region3, state8).
vertices(region3, exit5).
vertices(region3, finalState7).
vertices(region3, exit7).
vertices(region3, exit9).
vertices(state7, state11).
vertices(state7, finalState10).
vertices(state7, finalState11).
vertices(region4, exit11).
vertices(region4, finalState3).
vertices(region4, state4).
vertices(region4, state6).
vertices(region4, finalState6).
vertices(region4, state10).
vertices(region4, exit12).
vertices(region4, finalState12).
outgoingTransition(choice1, transition5).
outgoingTransition(exit1, transition3).
outgoingTransition(state2, transition4).
outgoingTransition(state2, transition6).
outgoingTransition(state3, transition14).
outgoingTransition(finalState3, transition8).
outgoingTransition(finalState3, transition10).
outgoingTransition(state4, transition9).
outgoingTransition(state8, transition1).
outgoingTransition(state9, transition2).
outgoingTransition(entry4, transition13).
incomingTransition(choice1, transition1).
incomingTransition(choice1, transition3).
incomingTransition(finalState1, transition4).
incomingTransition(finalState1, transition8).
incomingTransition(state1, transition2).
incomingTransition(state1, transition5).
incomingTransition(state2, transition9).
incomingTransition(finalState3, transition6).
incomingTransition(finalState3, transition7).
incomingTransition(exit4, transition11).
incomingTransition(exit4, transition12).
incomingTransition(exit7, transition13).
