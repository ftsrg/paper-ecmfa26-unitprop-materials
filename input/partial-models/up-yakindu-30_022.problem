
abstract class CompositeElement {
    contains Region[] regions
}

class Region {
    contains Vertex[] vertices opposite region
}

abstract class Vertex {
    container Region region opposite vertices
    contains Transition[] outgoingTransition opposite source
    Transition[] incomingTransition opposite target
}

class Transition {
    container Vertex source opposite outgoingTransition
    Vertex[1] target opposite incomingTransition
}

abstract class Pseudostate extends Vertex.

abstract class RegularState extends Vertex.

class Entry extends Pseudostate.

class Exit extends Pseudostate.

class Choice extends Pseudostate.

class FinalState extends RegularState.

class State extends RegularState, CompositeElement.

class Statechart extends CompositeElement.



pred entryInRegion(r, Entry e) <->
    vertices(r, e).

error noEntryInRegion(Region r) <->
    !entryInRegion(r, _).

error multipleEntryInRegion(r) <->
    Entry(e1),
    Entry(e2),
    vertices(r, e1),
    vertices(r, e2),
    e1 != e2.

error incomingToEntry(t, Entry e) <->
    target(t, e).

error noOutgoingTransitionFromEntry(Entry e) <->
    !source(_, e).

error multipleTransitionFromEntry(Entry e, t1, t2) <->
    outgoingTransition(e, t1),
    outgoingTransition(e, t2),
    t1 != t2.


error outgoingFromExit(t, Exit e) <->
    source(t, e).


error outgoingFromFinal(t, FinalState e) <->
    source(t, e).


pred stateInRegion(r, State s) <->
    vertices(r, s).

error noStateInRegion(Region r) <->
    !stateInRegion(r, _).


error choiceHasNoOutgoing(Choice c) <->
    !source(_, c).

error choiceHasNoIncoming(Choice c) <->
    !target(_, c).

declare sct, transition1, region1, state1, exit1, exit2, state2, transition2,
	transition3, region2, choice1, choice2, transition4, entry1, entry2,
	region3, finalState1, transition5, state3, exit3, finalState2,
	transition6, transition7, transition8, state4, transition9, finalState3,
	transition10, transition11, transition12, exit4, finalState4, state5,
	finalState5, state6, entry3.
Statechart(sct).
Region(region1).
State(state1).
Exit(exit1).
State(state2).
Transition(transition2).
Transition(transition3).
Region(region2).
Choice(choice2).
Transition(transition4).
Entry(entry1).
Entry(entry2).
Region(region3).
Transition(transition5).
State(state3).
FinalState(finalState2).
Transition(transition6).
Transition(transition7).
Transition(transition8).
State(state4).
Transition(transition9).
FinalState(finalState3).
Transition(transition10).
Transition(transition11).
Exit(exit4).
FinalState(finalState4).
State(state5).
FinalState(finalState5).
State(state6).
Entry(entry3).
regions(sct, region1).
regions(state1, region2).
vertices(region1, state1).
vertices(region1, exit1).
vertices(region1, state2).
vertices(region1, entry1).
vertices(region1, state3).
vertices(region1, exit3).
vertices(region1, finalState4).
vertices(region2, choice1).
vertices(region2, choice2).
vertices(region2, entry2).
vertices(region2, finalState1).
vertices(region2, state4).
vertices(region2, exit4).
vertices(region2, finalState5).
vertices(region3, finalState2).
vertices(region3, finalState3).
vertices(region3, state5).
vertices(region3, state6).
vertices(region3, entry3).
outgoingTransition(state1, transition3).
outgoingTransition(state2, transition2).
outgoingTransition(state2, transition10).
outgoingTransition(choice1, transition4).
outgoingTransition(choice1, transition12).
outgoingTransition(choice2, transition5).
outgoingTransition(choice2, transition8).
outgoingTransition(entry1, transition7).
outgoingTransition(state3, transition11).
outgoingTransition(state4, transition9).
outgoingTransition(entry3, transition1).
incomingTransition(state1, transition4).
incomingTransition(exit2, transition2).
incomingTransition(exit2, transition3).
incomingTransition(state2, transition12).
incomingTransition(choice1, transition1).
incomingTransition(choice2, transition9).
incomingTransition(exit3, transition6).
incomingTransition(finalState2, transition5).
incomingTransition(state4, transition8).
incomingTransition(finalState3, transition10).
