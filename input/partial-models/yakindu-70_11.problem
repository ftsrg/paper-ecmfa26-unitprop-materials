
abstract class CompositeElement {
    contains Region[] regions
}

class Region {
    contains Vertex[] vertices opposite region
}

abstract class Vertex {
    container Region region opposite vertices
    contains Transition[] outgoingTransition opposite source
    Transition[] incomingTransition opposite target
}

class Transition {
    container Vertex source opposite outgoingTransition
    Vertex[1] target opposite incomingTransition
}

abstract class Pseudostate extends Vertex.

abstract class RegularState extends Vertex.

class Entry extends Pseudostate.

class Exit extends Pseudostate.

class Choice extends Pseudostate.

class FinalState extends RegularState.

class State extends RegularState, CompositeElement.

class Statechart extends CompositeElement.



pred entryInRegion(r, Entry e) <->
    vertices(r, e).

error noEntryInRegion(Region r) <->
    !entryInRegion(r, _).

error multipleEntryInRegion(r) <->
    Entry(e1),
    Entry(e2),
    vertices(r, e1),
    vertices(r, e2),
    e1 != e2.

error incomingToEntry(t, Entry e) <->
    target(t, e).

error noOutgoingTransitionFromEntry(Entry e) <->
    !source(_, e).

error multipleTransitionFromEntry(Entry e, t1, t2) <->
    outgoingTransition(e, t1),
    outgoingTransition(e, t2),
    t1 != t2.


error outgoingFromExit(t, Exit e) <->
    source(t, e).


error outgoingFromFinal(t, FinalState e) <->
    source(t, e).


pred stateInRegion(r, State s) <->
    vertices(r, s).

error noStateInRegion(Region r) <->
    !stateInRegion(r, _).


error choiceHasNoOutgoing(Choice c) <->
    !source(_, c).

error choiceHasNoIncoming(Choice c) <->
    !target(_, c).

declare sct, region1, exit1, exit2, choice1, entry1, transition1, state1,
	finalState1, region2, state2, state3, exit3, state4, transition2, entry2,
	state5, finalState2, transition3, transition4, exit4, state6, transition5.
declare transition6, transition7, exit5, finalState3, state7, state8, entry3.
declare transition8, exit6, state9, state10, transition9.
declare transition10, exit7, state11, state12, entry4, transition11, exit8.
declare transition12, transition13, state13, exit9, exit10, exit11.
declare transition14, state14, transition15, transition16, exit12, entry5.
declare finalState4, transition17, finalState5, state15, exit13, exit14.
declare transition18, state16, region3, state17, entry6, transition19.
declare exit15, finalState6, state18, transition20, transition21.
declare transition22, region4, entry7, state19, exit16, transition23, choice2.
declare state20, exit17, transition24.
Statechart(sct).
Region(region1).
Exit(exit1).
Choice(exit2).
Choice(choice1).
Entry(entry1).
Transition(transition1).
State(state1).
Exit(finalState1).
Region(region2).
Region(state2).
Region(state3).
State(state4).
Transition(transition2).
Entry(entry2).
State(state5).
State(finalState2).
Transition(transition3).
Transition(transition4).
Exit(exit4).
Transition(transition5).
Transition(transition7).
Exit(exit5).
FinalState(finalState3).
State(state7).
Exit(state8).
Entry(entry3).
Transition(transition8).
FinalState(exit6).
State(state9).
Transition(transition10).
Exit(exit7).
State(state11).
Choice(state12).
Entry(entry4).
Region(transition11).
FinalState(exit8).
Transition(transition12).
State(state13).
Exit(exit10).
Transition(transition14).
State(state14).
Transition(transition15).
Transition(transition16).
Exit(exit12).
FinalState(finalState4).
Transition(transition17).
FinalState(finalState5).
State(state15).
Exit(exit13).
Exit(exit14).
Transition(transition18).
Region(region3).
State(state17).
Entry(entry6).
Transition(transition19).
Exit(exit15).
FinalState(finalState6).
State(state18).
Transition(transition20).
Transition(transition21).
Transition(transition22).
Region(region4).
Entry(entry7).
State(state19).
Exit(exit16).
Transition(transition23).
Choice(choice2).
State(state20).
Exit(exit17).
Transition(transition24).
regions(sct, region1).
regions(state1, state2).
regions(state1, region4).
regions(state5, region2).
regions(state6, transition11).
regions(state10, state3).
vertices(region1, choice1).
vertices(region1, entry1).
vertices(region1, finalState1).
vertices(region1, finalState2).
vertices(region1, finalState3).
vertices(region1, state7).
vertices(region1, state9).
vertices(region1, state10).
vertices(region1, exit8).
vertices(region1, exit11).
vertices(region2, state4).
vertices(region2, entry2).
vertices(region2, state6).
vertices(region2, state8).
vertices(region2, exit12).
vertices(region2, exit13).
vertices(state2, exit1).
vertices(state2, exit3).
vertices(state2, exit5).
vertices(state2, entry3).
vertices(state2, exit6).
vertices(state2, state15).
vertices(state3, exit2).
vertices(state3, state5).
vertices(state3, exit7).
vertices(state3, state12).
vertices(state3, entry4).
vertices(state3, exit9).
vertices(state3, finalState5).
vertices(state3, state16).
vertices(state3, finalState6).
vertices(state3, exit16).
vertices(transition11, state1).
vertices(transition11, exit14).
vertices(transition11, exit15).
vertices(region3, state17).
vertices(region3, entry6).
vertices(region3, state18).
vertices(region3, choice2).
vertices(region4, state13).
vertices(region4, state14).
vertices(region4, entry7).
vertices(region4, state20).
vertices(region4, exit17).
outgoingTransition(exit2, transition8).
outgoingTransition(exit2, transition12).
outgoingTransition(choice1, transition5).
outgoingTransition(choice1, transition22).
outgoingTransition(entry1, transition13).
outgoingTransition(state1, transition4).
outgoingTransition(state4, transition3).
outgoingTransition(entry2, transition17).
outgoingTransition(state6, transition1).
outgoingTransition(entry3, transition19).
outgoingTransition(state9, transition16).
outgoingTransition(state10, transition6).
outgoingTransition(state10, transition7).
outgoingTransition(state12, transition9).
outgoingTransition(state13, transition10).
outgoingTransition(state14, transition2).
outgoingTransition(entry5, transition14).
outgoingTransition(state15, transition18).
outgoingTransition(entry6, transition21).
outgoingTransition(entry7, transition23).
outgoingTransition(choice2, transition24).
incomingTransition(exit2, transition12).
incomingTransition(exit2, transition24).
incomingTransition(choice1, transition5).
incomingTransition(state1, transition3).
incomingTransition(finalState1, transition9).
incomingTransition(exit3, transition7).
incomingTransition(state4, transition6).
incomingTransition(state5, transition1).
incomingTransition(finalState2, transition4).
incomingTransition(exit4, transition15).
incomingTransition(exit4, transition17).
incomingTransition(state6, transition8).
incomingTransition(state9, transition2).
incomingTransition(state10, transition14).
incomingTransition(state11, transition21).
incomingTransition(state12, transition16).
incomingTransition(exit8, transition23).
incomingTransition(exit9, transition20).
incomingTransition(exit13, transition10).
incomingTransition(exit14, transition13).
incomingTransition(state16, transition18).
incomingTransition(state16, transition19).
incomingTransition(choice2, transition22).
