
abstract class CompositeElement {
    contains Region[] regions
}

class Region {
    contains Vertex[] vertices opposite region
}

abstract class Vertex {
    container Region region opposite vertices
    contains Transition[] outgoingTransition opposite source
    Transition[] incomingTransition opposite target
}

class Transition {
    container Vertex source opposite outgoingTransition
    Vertex[1] target opposite incomingTransition
}

abstract class Pseudostate extends Vertex.

abstract class RegularState extends Vertex.

class Entry extends Pseudostate.

class Exit extends Pseudostate.

class Choice extends Pseudostate.

class FinalState extends RegularState.

class State extends RegularState, CompositeElement.

class Statechart extends CompositeElement.



pred entryInRegion(r, Entry e) <->
    vertices(r, e).

error noEntryInRegion(Region r) <->
    !entryInRegion(r, _).

error multipleEntryInRegion(r) <->
    Entry(e1),
    Entry(e2),
    vertices(r, e1),
    vertices(r, e2),
    e1 != e2.

error incomingToEntry(t, Entry e) <->
    target(t, e).

error noOutgoingTransitionFromEntry(Entry e) <->
    !source(_, e).

error multipleTransitionFromEntry(Entry e, t1, t2) <->
    outgoingTransition(e, t1),
    outgoingTransition(e, t2),
    t1 != t2.


error outgoingFromExit(t, Exit e) <->
    source(t, e).


error outgoingFromFinal(t, FinalState e) <->
    source(t, e).


pred stateInRegion(r, State s) <->
    vertices(r, s).

error noStateInRegion(Region r) <->
    !stateInRegion(r, _).


error choiceHasNoOutgoing(Choice c) <->
    !source(_, c).

error choiceHasNoIncoming(Choice c) <->
    !target(_, c).

declare sct, region1, entry1, choice1, state1, state2, transition1, region2,
	finalState1, entry2, exit1, exit2, finalState2, finalState3, transition2,
	transition3, transition4, transition5, state3, exit3, finalState4, state4.
declare transition6, transition7, transition8, transition9, region3, state5,
	entry3, transition10.
declare transition11, exit4, finalState5, finalState6, transition12, state6.
declare transition13, region4, entry4, exit5, state7, transition14.
declare exit6.
declare transition15, transition16, choice2, region5, finalState7,
	transition17, transition18, state8, state9, state10, entry5.
declare state11, exit7, state12, state13, transition19.
declare transition20, finalState8, region6, state14, exit8, state15, entry6.
declare finalState9, state16, finalState10, transition21, finalState11,
	state17.
declare transition22, state18, finalState12, region7, transition23, entry7.
declare finalState13, finalState14, transition24.
Statechart(sct).
Region(region1).
Entry(entry1).
Choice(choice1).
State(state1).
Transition(transition1).
Region(region2).
State(finalState1).
Entry(entry2).
Exit(exit1).
FinalState(exit2).
Choice(finalState2).
FinalState(finalState3).
Transition(transition2).
Transition(transition3).
Transition(transition4).
Transition(transition5).
State(state3).
Exit(exit3).
FinalState(finalState4).
Transition(transition6).
Transition(transition7).
Transition(transition8).
Transition(transition9).
Region(region3).
State(state5).
Transition(transition10).
Transition(transition11).
Exit(exit4).
Choice(finalState5).
FinalState(finalState6).
Transition(transition12).
Exit(state6).
Transition(transition13).
Region(region4).
Entry(entry4).
Exit(exit5).
State(state7).
Transition(transition14).
Exit(exit6).
Transition(transition15).
Transition(transition16).
Region(region5).
FinalState(finalState7).
Transition(transition17).
Transition(transition18).
State(state9).
State(state10).
Entry(entry5).
State(state11).
Exit(exit7).
State(state12).
State(state13).
Transition(transition19).
Transition(transition20).
Region(region6).
State(state14).
Exit(exit8).
Entry(entry6).
FinalState(finalState9).
State(state16).
FinalState(finalState10).
Transition(transition21).
FinalState(finalState11).
Transition(transition22).
State(state18).
FinalState(finalState12).
Region(region7).
Transition(transition23).
Entry(entry7).
FinalState(finalState13).
FinalState(finalState14).
Transition(transition24).
regions(sct, region1).
regions(sct, region2).
regions(state1, region7).
regions(state2, region4).
regions(state10, region3).
regions(state10, region6).
vertices(region1, entry1).
vertices(region1, state1).
vertices(region1, state2).
vertices(region1, state3).
vertices(region1, finalState6).
vertices(region1, state6).
vertices(region1, exit6).
vertices(region1, state14).
vertices(region1, state16).
vertices(region1, state18).
vertices(region2, finalState1).
vertices(region2, entry2).
vertices(region2, finalState2).
vertices(region2, state8).
vertices(region2, state13).
vertices(region3, exit2).
vertices(region3, state5).
vertices(region3, entry3).
vertices(region3, exit4).
vertices(region3, exit5).
vertices(region3, finalState8).
vertices(region4, choice1).
vertices(region4, finalState4).
vertices(region4, entry4).
vertices(region4, state7).
vertices(region4, finalState7).
vertices(region4, exit7).
vertices(region4, state12).
vertices(region4, exit8).
vertices(region4, finalState10).
vertices(region4, finalState12).
vertices(region5, finalState3).
vertices(region5, state10).
vertices(region5, state11).
vertices(region5, finalState9).
vertices(region6, entry5).
vertices(region6, finalState11).
vertices(region6, state17).
vertices(region6, finalState13).
vertices(region6, finalState14).
vertices(region7, state4).
vertices(region7, state15).
vertices(region7, entry7).
outgoingTransition(entry1, transition11).
outgoingTransition(choice1, transition14).
outgoingTransition(choice1, transition15).
outgoingTransition(state1, transition3).
outgoingTransition(state1, transition7).
outgoingTransition(state1, transition9).
outgoingTransition(state2, transition8).
outgoingTransition(state2, transition17).
outgoingTransition(finalState1, transition16).
outgoingTransition(entry2, transition24).
outgoingTransition(finalState2, transition6).
outgoingTransition(state4, transition2).
outgoingTransition(state5, transition10).
outgoingTransition(state5, transition12).
outgoingTransition(entry3, transition21).
outgoingTransition(finalState5, transition23).
outgoingTransition(entry4, transition13).
outgoingTransition(state7, transition4).
outgoingTransition(choice2, transition18).
outgoingTransition(entry5, transition1).
outgoingTransition(state11, transition19).
outgoingTransition(entry6, transition20).
outgoingTransition(entry7, transition22).
incomingTransition(choice1, transition10).
incomingTransition(state1, transition1).
incomingTransition(state1, transition16).
incomingTransition(state1, transition19).
incomingTransition(finalState1, transition2).
incomingTransition(finalState1, transition9).
incomingTransition(finalState1, transition12).
incomingTransition(finalState2, transition8).
incomingTransition(state3, transition6).
incomingTransition(exit3, transition11).
incomingTransition(state4, transition7).
incomingTransition(finalState5, transition14).
incomingTransition(exit6, transition15).
incomingTransition(choice2, transition5).
incomingTransition(finalState7, transition18).
incomingTransition(state8, transition23).
incomingTransition(state9, transition4).
incomingTransition(finalState8, transition20).
incomingTransition(state15, transition21).
incomingTransition(finalState9, transition3).
incomingTransition(finalState12, transition24).
