
abstract class CompositeElement {
    contains Region[] regions
}

class Region {
    contains Vertex[] vertices opposite region
}

abstract class Vertex {
    container Region region opposite vertices
    contains Transition[] outgoingTransition opposite source
    Transition[] incomingTransition opposite target
}

class Transition {
    container Vertex source opposite outgoingTransition
    Vertex[1] target opposite incomingTransition
}

abstract class Pseudostate extends Vertex.

abstract class RegularState extends Vertex.

class Entry extends Pseudostate.

class Exit extends Pseudostate.

class Choice extends Pseudostate.

class FinalState extends RegularState.

class State extends RegularState, CompositeElement.

class Statechart extends CompositeElement.



pred entryInRegion(r, Entry e) <->
    vertices(r, e).

error noEntryInRegion(Region r) <->
    !entryInRegion(r, _).

error multipleEntryInRegion(r) <->
    Entry(e1),
    Entry(e2),
    vertices(r, e1),
    vertices(r, e2),
    e1 != e2.

error incomingToEntry(t, Entry e) <->
    target(t, e).

error noOutgoingTransitionFromEntry(Entry e) <->
    !source(_, e).

error multipleTransitionFromEntry(Entry e, t1, t2) <->
    outgoingTransition(e, t1),
    outgoingTransition(e, t2),
    t1 != t2.


error outgoingFromExit(t, Exit e) <->
    source(t, e).


error outgoingFromFinal(t, FinalState e) <->
    source(t, e).


pred stateInRegion(r, State s) <->
    vertices(r, s).

error noStateInRegion(Region r) <->
    !stateInRegion(r, _).


error choiceHasNoOutgoing(Choice c) <->
    !source(_, c).

error choiceHasNoIncoming(Choice c) <->
    !target(_, c).

declare sct, transition1, region1, entry1, exit1, region2, exit2, transition2,
	entry2, state1, exit3, choice1, finalState1, exit4, transition3,
	finalState2, finalState3, exit5, transition4, state2, exit6, state3,
	finalState4, transition5.
declare transition6, region3, state4, state5, transition7, entry3.
declare transition8, finalState5, state6.
declare finalState6, region4, finalState7, state7, entry4, transition9.
declare transition10, exit7, exit8, finalState8, exit9, state8, state9,
	transition11, state10.
declare transition12, region5, state11, entry5, transition13, state12.
declare transition14, exit10, state13, state14, state15, exit11.
declare transition15, exit12, transition16, region6, entry6, finalState9.
declare state16, state17.
Statechart(sct).
Region(region1).
Entry(entry1).
Exit(exit1).
Region(region2).
Exit(exit2).
Transition(transition2).
Entry(entry2).
State(state1).
Exit(exit3).
Choice(choice1).
FinalState(finalState1).
Choice(exit4).
FinalState(finalState3).
Exit(exit5).
Transition(transition4).
State(state2).
Statechart(state3).
State(finalState4).
Transition(transition5).
Transition(transition6).
Region(region3).
Exit(state4).
State(state5).
Transition(transition7).
Entry(entry3).
Transition(transition8).
State(finalState5).
State(state6).
FinalState(finalState6).
Region(region4).
Exit(finalState7).
State(state7).
Entry(entry4).
Transition(transition9).
Transition(transition10).
Exit(exit8).
FinalState(finalState8).
Exit(exit9).
State(state8).
State(state9).
Transition(transition11).
Transition(state10).
Transition(transition12).
Region(region5).
Entry(entry5).
Transition(transition13).
State(state12).
Transition(transition14).
Exit(exit10).
State(state13).
Exit(state14).
State(state15).
Exit(exit11).
Transition(transition15).
Exit(exit12).
Transition(transition16).
Region(region6).
Entry(entry6).
FinalState(finalState9).
State(state16).
State(state17).
regions(sct, region2).
regions(sct, region6).
regions(state2, region1).
regions(finalState4, region3).
regions(finalState4, region4).
regions(state5, region5).
vertices(region1, entry1).
vertices(region1, finalState1).
vertices(region1, exit7).
vertices(region1, state17).
vertices(region2, exit2).
vertices(region2, state1).
vertices(region2, exit3).
vertices(region2, exit4).
vertices(region2, exit5).
vertices(region2, exit6).
vertices(region2, finalState4).
vertices(region2, state4).
vertices(region2, entry3).
vertices(region2, finalState7).
vertices(region2, state14).
vertices(region3, exit1).
vertices(region3, state5).
vertices(region3, finalState6).
vertices(region3, state7).
vertices(region3, exit8).
vertices(region3, finalState8).
vertices(region3, state8).
vertices(region3, exit11).
vertices(region4, state2).
vertices(region4, finalState5).
vertices(region4, entry4).
vertices(region4, exit9).
vertices(region4, state9).
vertices(region4, state12).
vertices(region4, state15).
vertices(region4, finalState9).
vertices(region4, state16).
vertices(region5, finalState2).
vertices(region5, state11).
vertices(region5, entry5).
vertices(region6, state6).
vertices(region6, entry6).
outgoingTransition(entry1, transition2).
outgoingTransition(state1, transition1).
outgoingTransition(choice1, transition3).
outgoingTransition(state2, transition4).
outgoingTransition(state2, transition10).
outgoingTransition(state5, transition7).
outgoingTransition(state5, transition8).
outgoingTransition(entry3, transition6).
outgoingTransition(state6, transition11).
outgoingTransition(entry4, transition9).
outgoingTransition(exit7, transition5).
outgoingTransition(exit7, transition14).
outgoingTransition(state8, state10).
outgoingTransition(entry5, transition13).
outgoingTransition(entry6, transition15).
incomingTransition(exit1, transition5).
incomingTransition(exit1, transition9).
incomingTransition(exit3, transition4).
incomingTransition(exit3, transition13).
incomingTransition(choice1, transition10).
incomingTransition(choice1, transition14).
incomingTransition(finalState1, transition3).
incomingTransition(finalState1, transition6).
incomingTransition(exit4, transition8).
incomingTransition(exit4, transition12).
incomingTransition(finalState3, transition7).
incomingTransition(exit6, transition2).
incomingTransition(finalState4, transition1).
incomingTransition(state4, transition11).
incomingTransition(exit7, state10).
incomingTransition(state9, transition15).
