
abstract class CompositeElement {
    contains Region[] regions
}

class Region {
    contains Vertex[] vertices opposite region
}

abstract class Vertex {
    container Region region opposite vertices
    contains Transition[] outgoingTransition opposite source
    Transition[] incomingTransition opposite target
}

class Transition {
    container Vertex source opposite outgoingTransition
    Vertex[1] target opposite incomingTransition
}

abstract class Pseudostate extends Vertex.

abstract class RegularState extends Vertex.

class Entry extends Pseudostate.

class Exit extends Pseudostate.

class Choice extends Pseudostate.

class FinalState extends RegularState.

class State extends RegularState, CompositeElement.

class Statechart extends CompositeElement.



pred entryInRegion(r, Entry e) <->
    vertices(r, e).

error noEntryInRegion(Region r) <->
    !entryInRegion(r, _).

error multipleEntryInRegion(r) <->
    Entry(e1),
    Entry(e2),
    vertices(r, e1),
    vertices(r, e2),
    e1 != e2.

error incomingToEntry(t, Entry e) <->
    target(t, e).

error noOutgoingTransitionFromEntry(Entry e) <->
    !source(_, e).

error multipleTransitionFromEntry(Entry e, t1, t2) <->
    outgoingTransition(e, t1),
    outgoingTransition(e, t2),
    t1 != t2.


error outgoingFromExit(t, Exit e) <->
    source(t, e).


error outgoingFromFinal(t, FinalState e) <->
    source(t, e).


pred stateInRegion(r, State s) <->
    vertices(r, s).

error noStateInRegion(Region r) <->
    !stateInRegion(r, _).


error choiceHasNoOutgoing(Choice c) <->
    !source(_, c).

error choiceHasNoIncoming(Choice c) <->
    !target(_, c).

declare sct, transition1, region1, region2, entry1, choice1, exit1, exit2,
	state1, exit3, exit4, state2, transition2, finalState1, transition3,
	exit5, transition4, state3, exit6, entry2, transition5, state4, state5,
	state6.
declare transition6, transition7, state7, entry3, transition8, entry4.
declare transition9, state8.
declare transition10, exit7, transition11, exit8, finalState2, region3,
	state9, state10, transition12, entry5.
declare transition13, exit9, transition14.
declare transition15, transition16, region4, state11, transition17, entry6,
	transition18, state12, choice2.
Statechart(sct).
Transition(transition1).
Region(region1).
Region(region2).
Choice(entry1).
Choice(choice1).
Exit(exit1).
Region(exit2).
State(exit3).
Exit(exit4).
State(state2).
Transition(transition2).
FinalState(finalState1).
Transition(transition3).
Exit(exit5).
State(state3).
node(exit6).
!CompositeElement(exit6).
!Region(exit6).
!Vertex(exit6).
!Transition(exit6).
Entry(entry2).
Transition(transition5).
State(state4).
State(state5).
State(state6).
Transition(transition6).
State(state7).
Transition(transition8).
Entry(entry4).
Transition(transition9).
State(state8).
Transition(transition10).
Transition(transition11).
Exit(exit8).
Region(region3).
State(state9).
State(state10).
Transition(transition12).
Entry(entry5).
Transition(transition13).
Exit(exit9).
Transition(transition14).
Transition(transition15).
Region(region4).
State(state11).
Transition(transition17).
Entry(entry6).
State(state12).
Choice(choice2).
regions(sct, region1).
regions(sct, region2).
regions(state3, exit2).
regions(state5, region3).
regions(state10, region4).
vertices(region1, state1).
vertices(region1, exit3).
vertices(region1, state3).
vertices(region1, entry2).
vertices(region1, state4).
vertices(region1, state6).
vertices(region1, state8).
vertices(region1, exit8).
vertices(region1, state9).
vertices(region2, choice1).
vertices(region2, exit1).
vertices(region2, state2).
vertices(region2, state5).
vertices(region2, exit7).
vertices(region2, choice2).
vertices(exit2, entry1).
vertices(exit2, exit4).
vertices(exit2, finalState1).
vertices(exit2, state7).
vertices(exit2, entry3).
vertices(exit2, finalState2).
vertices(region3, exit5).
vertices(region3, state10).
vertices(region3, entry5).
vertices(region3, exit9).
vertices(region4, entry4).
vertices(region4, state11).
vertices(region4, state12).
outgoingTransition(entry1, transition17).
outgoingTransition(choice1, transition8).
outgoingTransition(choice1, transition14).
outgoingTransition(choice1, transition16).
outgoingTransition(state1, transition1).
outgoingTransition(entry2, transition13).
outgoingTransition(state4, transition3).
outgoingTransition(state4, transition4).
outgoingTransition(state5, transition6).
outgoingTransition(entry3, transition9).
outgoingTransition(entry4, transition12).
outgoingTransition(state9, transition7).
outgoingTransition(entry5, transition10).
outgoingTransition(state11, transition5).
outgoingTransition(entry6, transition18).
outgoingTransition(choice2, transition15).
incomingTransition(entry1, transition10).
incomingTransition(choice1, transition1).
incomingTransition(state1, transition3).
incomingTransition(state1, transition5).
incomingTransition(exit4, transition2).
incomingTransition(state2, transition8).
incomingTransition(state3, transition6).
incomingTransition(state4, transition7).
incomingTransition(state4, transition9).
incomingTransition(state4, transition11).
incomingTransition(state4, transition17).
incomingTransition(state5, transition14).
incomingTransition(exit7, transition16).
incomingTransition(exit7, transition18).
incomingTransition(exit8, transition4).
incomingTransition(choice2, transition15).
