
abstract class CompositeElement {
    contains Region[] regions
}

class Region {
    contains Vertex[] vertices opposite region
}

abstract class Vertex {
    container Region region opposite vertices
    contains Transition[] outgoingTransition opposite source
    Transition[] incomingTransition opposite target
}

class Transition {
    container Vertex source opposite outgoingTransition
    Vertex[1] target opposite incomingTransition
}

abstract class Pseudostate extends Vertex.

abstract class RegularState extends Vertex.

class Entry extends Pseudostate.

class Exit extends Pseudostate.

class Choice extends Pseudostate.

class FinalState extends RegularState.

class State extends RegularState, CompositeElement.

class Statechart extends CompositeElement.



pred entryInRegion(r, Entry e) <->
    vertices(r, e).

error noEntryInRegion(Region r) <->
    !entryInRegion(r, _).

error multipleEntryInRegion(r) <->
    Entry(e1),
    Entry(e2),
    vertices(r, e1),
    vertices(r, e2),
    e1 != e2.

error incomingToEntry(t, Entry e) <->
    target(t, e).

error noOutgoingTransitionFromEntry(Entry e) <->
    !source(_, e).

error multipleTransitionFromEntry(Entry e, t1, t2) <->
    outgoingTransition(e, t1),
    outgoingTransition(e, t2),
    t1 != t2.


error outgoingFromExit(t, Exit e) <->
    source(t, e).


error outgoingFromFinal(t, FinalState e) <->
    source(t, e).


pred stateInRegion(r, State s) <->
    vertices(r, s).

error noStateInRegion(Region r) <->
    !stateInRegion(r, _).


error choiceHasNoOutgoing(Choice c) <->
    !source(_, c).

error choiceHasNoIncoming(Choice c) <->
    !target(_, c).

declare sct, transition1, region1, finalState1, exit1, choice1, transition2,
	entry1, exit2, finalState2, transition3, transition4, state1, state2,
	region2, exit3, exit4, transition5, transition6, state3, entry2, state4,
	transition7, state5.
declare region3, entry3, transition8, exit5, transition9, state6.
declare transition10, transition11, transition12, transition13, finalState3,
	finalState4, state7, state8, entry4, transition14.
declare state9, state10, state11, finalState5, state12, entry5, exit6,
	transition15.
declare transition16, exit7, region4, state13, entry6, transition17.
declare state14, finalState6.
declare region5, finalState7, entry7, state15, transition18, transition19.
declare finalState8, state16.
declare transition20, region6, exit8, state17, exit9, state18, entry8,
	state19, transition21, exit10, exit11, state20, exit12, exit13.
declare transition22, transition23, finalState9, state21, state22,
	finalState10.
declare entry9, transition24, state23, exit14, region7, state24.
declare state25.
Statechart(sct).
Transition(transition1).
Region(region1).
State(finalState1).
Exit(exit1).
Choice(choice1).
Transition(transition2).
Entry(entry1).
Exit(exit2).
Exit(finalState2).
Transition(transition3).
Transition(transition4).
State(state1).
State(state2).
Region(region2).
Exit(exit3).
Transition(transition5).
Transition(transition6).
State(state3).
Entry(entry2).
Choice(state4).
Transition(transition7).
Exit(state5).
Region(region3).
State(entry3).
Transition(transition8).
Exit(exit5).
Transition(transition9).
State(state6).
Transition(transition11).
Transition(transition12).
Transition(transition13).
Choice(finalState3).
FinalState(finalState4).
State(state7).
FinalState(state8).
Entry(entry4).
Transition(transition14).
State(state9).
State(state10).
Choice(state11).
Exit(finalState5).
State(state12).
Entry(entry5).
Transition(transition15).
Transition(transition16).
Exit(exit7).
Region(region4).
State(state13).
Transition(transition17).
State(state14).
node(finalState6).
!Region(finalState6).
!Vertex(finalState6).
!Transition(finalState6).
Region(region5).
FinalState(finalState7).
Entry(entry7).
State(state15).
Transition(transition18).
Transition(transition19).
FinalState(finalState8).
State(state16).
Transition(transition20).
Region(region6).
State(state17).
Transition(state18).
Entry(entry8).
State(state19).
Transition(transition21).
Exit(exit10).
Exit(exit11).
State(state20).
Exit(exit12).
Exit(exit13).
Transition(transition22).
Transition(transition23).
FinalState(finalState9).
State(state21).
State(state22).
FinalState(finalState10).
Entry(entry9).
Transition(transition24).
State(state23).
Exit(exit14).
Region(region7).
State(state24).
State(state25).
regions(sct, region1).
regions(sct, region7).
regions(state2, region5).
regions(state3, region6).
regions(entry3, region3).
regions(state9, region4).
regions(state10, region2).
regions(state14, exit4).
vertices(region1, exit1).
vertices(region1, finalState2).
vertices(region1, exit3).
vertices(region1, state3).
vertices(region1, state5).
vertices(region1, exit5).
vertices(region1, finalState3).
vertices(region1, state7).
vertices(region1, entry4).
vertices(region1, state14).
vertices(region1, state21).
vertices(region2, finalState1).
vertices(region2, entry2).
vertices(region2, state4).
vertices(region2, finalState4).
vertices(region2, state8).
vertices(region2, exit10).
vertices(region2, finalState9).
vertices(region2, exit14).
vertices(exit4, state10).
vertices(exit4, finalState5).
vertices(exit4, entry5).
vertices(exit4, exit6).
vertices(exit4, state16).
vertices(exit4, state17).
vertices(exit4, exit9).
vertices(exit4, exit11).
vertices(exit4, state20).
vertices(exit4, state22).
vertices(exit4, state23).
vertices(region3, entry1).
vertices(region3, exit2).
vertices(region3, state2).
vertices(region3, state12).
vertices(region3, exit7).
vertices(region3, state13).
vertices(region4, state11).
vertices(region4, entry6).
vertices(region4, finalState10).
vertices(region4, state25).
vertices(region5, state15).
vertices(region5, finalState8).
vertices(region5, state19).
vertices(region5, exit13).
vertices(region6, state1).
vertices(region6, finalState7).
vertices(region6, entry8).
vertices(region7, entry9).
vertices(region7, state24).
outgoingTransition(finalState1, transition1).
outgoingTransition(finalState1, transition12).
outgoingTransition(choice1, transition3).
outgoingTransition(choice1, transition22).
outgoingTransition(entry1, transition4).
outgoingTransition(state1, transition2).
outgoingTransition(state1, transition8).
outgoingTransition(state2, transition5).
outgoingTransition(entry2, transition7).
outgoingTransition(entry3, transition6).
outgoingTransition(finalState3, transition20).
outgoingTransition(state11, transition18).
outgoingTransition(entry6, transition17).
outgoingTransition(state15, transition9).
outgoingTransition(state15, transition15).
outgoingTransition(state16, transition11).
outgoingTransition(entry8, transition21).
outgoingTransition(state22, transition19).
outgoingTransition(entry9, state18).
incomingTransition(finalState1, transition2).
incomingTransition(finalState1, transition17).
incomingTransition(finalState1, transition21).
incomingTransition(exit1, transition3).
incomingTransition(exit1, transition8).
incomingTransition(exit1, transition10).
incomingTransition(choice1, transition1).
incomingTransition(exit2, transition5).
incomingTransition(finalState2, transition15).
incomingTransition(state2, transition9).
incomingTransition(state2, transition14).
incomingTransition(exit3, transition4).
incomingTransition(exit3, transition7).
incomingTransition(exit3, state18).
incomingTransition(state4, transition6).
incomingTransition(state5, transition11).
incomingTransition(exit5, transition13).
incomingTransition(state6, transition24).
incomingTransition(finalState4, transition19).
incomingTransition(state8, transition12).
incomingTransition(state10, transition18).
incomingTransition(state10, transition23).
incomingTransition(state11, transition22).
incomingTransition(exit6, transition16).
