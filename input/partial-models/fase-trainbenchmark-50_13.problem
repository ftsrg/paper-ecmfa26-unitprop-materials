class RailwayContainer {
    contains Route[] routes
    contains Region[] regions
}
class Region {
    contains Sensor[] sensors
    contains TrackElement elements
}
class Semaphore {
    Signal signal
}
class Route {
    contains SwitchPosition[] follows opposite route
    Sensor[2..*] requires
    Semaphore entry
    Semaphore exit
    boolean active
}
class SwitchPosition {
    container Route route opposite follows
    Switch[1] target opposite positions
    Position position
}
abstract class TrackElement {
    Sensor[] monitoredBy opposite monitors
    TrackElement[] connectsTo
}
class Sensor {
    TrackElement[] monitors opposite monitoredBy
}
class Segment extends TrackElement {
    contains Semaphore[] semaphores
    int length
}
class Switch extends TrackElement {
    SwitchPosition[] positions opposite target
    Position currentPosition
    TrackElement straight
    TrackElement diverging
}
enum Position {
    FAILURE,
    STRAIGHT,
    DIVERGING
}
enum Signal {
    FAILURE,
    STOP,
    GO
}


error sameStraightDiverging(switch, target) <->
    straight(switch,target),
    diverging(switch,target).

error noDiverging(switch) <->
    Switch(switch),
    !diverging(switch,_).

error noStraight(switch) <->
    Switch(switch),
    !straight(switch,_).

error tooManyConncetions(segment) <->
    Segment(segment),
    count{connectsTo(segment, _)} > 2.


error selfLoop(e) <->
    TrackElement(e),
    connectsTo(e, e)
;
    Switch(e),
    straight(e, e)
;
    Switch(e),
    diverging(e, e).


error underconnectedSwitch(sw) <->
    Switch(sw),
    !switchWithTwoConnections(sw, _, _).

pred switchWithTwoConnections(sw, te1, te2) <->
    Switch(sw),
    connectsTo(sw, te1),
    connectsTo(sw, te2),
    te1 != te2.


error sensorWithoutElement(sensor) <->
    Sensor(sensor),
    !monitors(sensor, _).

error sensorMonitorsMultipleSwitches(sensor, sw1, sw2) <->
    Sensor(sensor),
    monitors(sensor, sw1),
    monitors(sensor, sw2),
    Switch(sw1),
    Switch(sw2),
    sw1 != sw2.

error duplicateMonitors(region, sensor1, sensor2, te) <->
    Region(region),
    sensors(region, sensor1),
    sensors(region, sensor2),
    sensor1 != sensor2,
    monitors(sensor1, te),
    monitors(sensor2, te).

error emptyRegion(region) <->
    Region(region),
    !elements(region, _).


error posLength(segment) <->
    Segment(segment),
    length(segment) <= 0.

error switchMonitored(sw) <-> 
    Switch(sw),
    !monitoredBy(sw, _).


error routeSensor(route, sensor, swP, sw) <->
    monitoredBy(sw, sensor),
    target(swP, sw),
    follows(route, swP),
    !requires(route, sensor).

error switchSet(semaphore, route, swP, sw) <->
    active(route),
    entry(route, semaphore),
    follows(route, swP),
    target(swP, sw),

    signal(semaphore, GO),
    position(swP, swpPosition),
    currentPosition(sw, swCurrentPosition),
    swCurrentPosition != swpPosition.



error connectedSegments(sensor, segment1, segment2, segment3, segment4, segment5, segment6) <->
    connectsTo(segment1, segment2),
    connectsTo(segment2, segment3),
    connectsTo(segment3, segment4),
    connectsTo(segment4, segment5),
    connectsTo(segment5, segment6),
    monitoredBy(segment1, sensor),
    monitoredBy(segment2, sensor),
    monitoredBy(segment3, sensor),
    monitoredBy(segment4, sensor),
    monitoredBy(segment5, sensor),
    monitoredBy(segment6, sensor).

error semaphoreNeighbor(route1, route2) <->
    exit(route1, semaphore),
    requires(route1, sensor1),
    monitoredBy(te1, sensor1),
    connectsTo(te1, te2),
    monitoredBy(te2, sensor2),
    requires(route2, sensor2),
    !entry(route2, semaphore),
    route1 != route2.

declare railwayContainer1, region1, switch1, region2, segment1, region3,
	region4, segment2, sensor1, semaphore1, region5, segment3, sensor2,
	switch2, sensor3, semaphore2, semaphore3, semaphore4.
declare region6, semaphore5, switch3, semaphore6, sensor4, semaphore7.
declare semaphore8.
declare region7, segment4, sensor5, sensor6, semaphore9, region8, sensor7,
	semaphore10, switch4, semaphore11, semaphore12.
declare semaphore13.
declare sensor8, switch5, segment5, semaphore14, semaphore15, semaphore16,
	semaphore17, region9, semaphore18, region10, semaphore19.
declare region11, semaphore20, segment6, semaphore21, semaphore22,
	semaphore23.
Region(region11).
RailwayContainer(railwayContainer1).
Region(region1).
Region(region2).
Segment(segment1).
Region(region3).
Region(region4).
Segment(segment2).
Sensor(sensor1).
Semaphore(semaphore1).
Region(region5).
Segment(segment3).
Sensor(sensor2).
Switch(switch2).
Sensor(sensor3).
Semaphore(semaphore4).
Region(region6).
Switch(switch3).
Semaphore(semaphore6).
Sensor(sensor4).
Semaphore(semaphore7).
Region(region7).
Segment(segment4).
Sensor(sensor5).
Semaphore(semaphore9).
Region(region8).
Sensor(sensor7).
Semaphore(semaphore10).
Switch(switch4).
Semaphore(semaphore11).
Semaphore(semaphore12).
Semaphore(semaphore13).
Sensor(sensor8).
Switch(switch5).
Segment(segment5).
Semaphore(semaphore14).
Semaphore(semaphore16).
Region(region9).
Semaphore(semaphore18).
Region(region10).
Semaphore(semaphore19).
Semaphore(semaphore20).
Segment(segment6).
Semaphore(semaphore21).
Semaphore(semaphore22).
Semaphore(semaphore23).
regions(railwayContainer1, region11).
regions(railwayContainer1, region1).
regions(railwayContainer1, region2).
regions(railwayContainer1, region3).
regions(railwayContainer1, region4).
regions(railwayContainer1, region5).
regions(railwayContainer1, region6).
regions(railwayContainer1, region7).
regions(railwayContainer1, region8).
regions(railwayContainer1, region9).
regions(railwayContainer1, region10).
sensors(region1, sensor3).
sensors(region1, sensor4).
sensors(region2, sensor2).
sensors(region3, sensor6).
sensors(region5, sensor8).
sensors(region7, sensor7).
elements(region11, segment3).
elements(region1, segment5).
elements(region2, segment1).
elements(region3, segment2).
elements(region4, segment6).
elements(region5, switch3).
elements(region6, switch2).
elements(region7, segment4).
elements(region8, switch4).
elements(region9, switch1).
elements(region10, switch5).
semaphores(segment1, semaphore6).
semaphores(segment1, semaphore8).
semaphores(segment1, semaphore13).
semaphores(segment1, semaphore14).
semaphores(segment2, semaphore2).
semaphores(segment2, semaphore12).
semaphores(segment2, semaphore16).
semaphores(segment2, semaphore23).
semaphores(segment3, semaphore1).
semaphores(segment3, semaphore7).
semaphores(segment3, semaphore9).
semaphores(segment3, semaphore18).
semaphores(segment3, semaphore20).
semaphores(segment3, semaphore21).
semaphores(segment3, semaphore22).
semaphores(segment4, semaphore5).
semaphores(segment4, semaphore10).
semaphores(segment4, semaphore17).
semaphores(segment5, semaphore11).
semaphores(segment5, semaphore19).
signal(semaphore2, GO).
signal(semaphore3, Signal::FAILURE).
signal(semaphore4, STOP).
signal(semaphore5, Signal::FAILURE).
signal(semaphore8, Signal::FAILURE).
signal(semaphore9, STOP).
signal(semaphore10, GO).
signal(semaphore11, Signal::FAILURE).
signal(semaphore12, Signal::FAILURE).
signal(semaphore13, Signal::FAILURE).
signal(semaphore14, STOP).
signal(semaphore15, GO).
signal(semaphore16, GO).
signal(semaphore17, STOP).
signal(semaphore19, GO).
signal(semaphore22, STOP).
signal(semaphore23, Signal::FAILURE).
monitoredBy(switch1, sensor4).
monitoredBy(segment1, sensor1).
monitoredBy(segment1, sensor6).
monitoredBy(segment1, sensor7).
monitoredBy(segment2, sensor1).
monitoredBy(segment2, sensor3).
monitoredBy(segment2, sensor6).
monitoredBy(segment2, sensor7).
monitoredBy(segment3, sensor2).
monitoredBy(segment3, sensor3).
monitoredBy(segment3, sensor5).
monitoredBy(segment3, sensor7).
monitoredBy(switch2, sensor1).
monitoredBy(switch2, sensor3).
monitoredBy(switch2, sensor5).
monitoredBy(switch2, sensor8).
monitoredBy(switch3, sensor2).
monitoredBy(segment4, sensor1).
monitoredBy(segment4, sensor2).
monitoredBy(segment4, sensor4).
monitoredBy(segment4, sensor5).
monitoredBy(segment4, sensor7).
monitoredBy(segment4, sensor8).
monitoredBy(switch4, sensor6).
monitoredBy(switch5, sensor7).
monitoredBy(segment5, sensor2).
monitoredBy(segment5, sensor3).
monitoredBy(segment5, sensor5).
monitoredBy(segment5, sensor7).
monitoredBy(segment6, sensor6).
connectsTo(switch1, segment1).
connectsTo(switch1, switch2).
connectsTo(switch1, switch3).
connectsTo(switch1, switch4).
connectsTo(switch1, switch5).
connectsTo(switch1, segment5).
connectsTo(segment1, segment2).
connectsTo(segment2, switch1).
connectsTo(segment2, segment4).
connectsTo(segment3, switch1).
connectsTo(segment3, segment2).
connectsTo(switch2, switch1).
connectsTo(switch2, segment1).
connectsTo(switch2, segment2).
connectsTo(switch2, segment3).
connectsTo(switch2, switch3).
connectsTo(switch2, switch4).
connectsTo(switch2, segment5).
connectsTo(switch3, switch1).
connectsTo(switch3, segment1).
connectsTo(switch3, segment2).
connectsTo(switch3, segment3).
connectsTo(switch3, segment4).
connectsTo(switch3, switch4).
connectsTo(segment4, switch1).
connectsTo(segment4, switch4).
connectsTo(switch4, switch1).
connectsTo(switch4, segment1).
connectsTo(switch4, segment2).
connectsTo(switch4, segment3).
connectsTo(switch4, switch2).
connectsTo(switch4, switch3).
connectsTo(switch4, segment4).
connectsTo(switch4, segment5).
connectsTo(switch5, switch1).
connectsTo(switch5, segment2).
connectsTo(switch5, segment3).
connectsTo(switch5, switch2).
connectsTo(switch5, switch3).
connectsTo(switch5, segment4).
connectsTo(switch5, segment5).
connectsTo(segment5, segment2).
connectsTo(segment5, switch4).
connectsTo(segment6, segment3).
currentPosition(switch1, STRAIGHT).
currentPosition(switch3, Position::FAILURE).
currentPosition(switch4, DIVERGING).
currentPosition(switch5, STRAIGHT).
straight(switch1, switch3).
straight(switch2, switch1).
straight(switch3, segment3).
straight(switch4, segment2).
straight(switch5, segment3).
diverging(switch1, segment1).
diverging(switch2, segment1).
diverging(switch3, segment2).
diverging(switch4, segment4).
diverging(switch5, switch3).
?length(segment1).
?length(segment2).
?length(segment3).
?length(segment4).
?length(segment5).
?length(segment6).

default !connectsTo(*, *).
?connectsTo(*, Segment::new).
?connectsTo(Segment::new, *).

default !monitoredBy(*, *).
?monitoredBy(Sensor::new, *).
