
abstract class CompositeElement {
    contains Region[] regions
}

class Region {
    contains Vertex[] vertices opposite region
}

abstract class Vertex {
    container Region region opposite vertices
    contains Transition[] outgoingTransition opposite source
    Transition[] incomingTransition opposite target
}

class Transition {
    container Vertex source opposite outgoingTransition
    Vertex[1] target opposite incomingTransition
}

abstract class Pseudostate extends Vertex.

abstract class RegularState extends Vertex.

class Entry extends Pseudostate.

class Exit extends Pseudostate.

class Choice extends Pseudostate.

class FinalState extends RegularState.

class State extends RegularState, CompositeElement.

class Statechart extends CompositeElement.



pred entryInRegion(r, Entry e) <->
    vertices(r, e).

error noEntryInRegion(Region r) <->
    !entryInRegion(r, _).

error multipleEntryInRegion(r) <->
    Entry(e1),
    Entry(e2),
    vertices(r, e1),
    vertices(r, e2),
    e1 != e2.

error incomingToEntry(t, Entry e) <->
    target(t, e).

error noOutgoingTransitionFromEntry(Entry e) <->
    !source(_, e).

error multipleTransitionFromEntry(Entry e, t1, t2) <->
    outgoingTransition(e, t1),
    outgoingTransition(e, t2),
    t1 != t2.


error outgoingFromExit(t, Exit e) <->
    source(t, e).


error outgoingFromFinal(t, FinalState e) <->
    source(t, e).


pred stateInRegion(r, State s) <->
    vertices(r, s).

error noStateInRegion(Region r) <->
    !stateInRegion(r, _).


error choiceHasNoOutgoing(Choice c) <->
    !source(_, c).

error choiceHasNoIncoming(Choice c) <->
    !target(_, c).

declare sct, state1, region1, choice1, finalState1, region2, transition1,
	exit1, state2, state3, finalState2, exit2, exit3, transition2, exit4,
	exit5, transition3, entry1, transition4, transition5, entry2, finalState3,
	transition6, exit6.
declare transition7, exit7, state4, transition8, state5, entry3.
declare state6, exit8.
declare transition9, finalState4, exit9, region3, state7, exit10,
	transition10, exit11, exit12, entry4.
declare transition11, state8, state9, finalState5, exit13, state10.
declare transition12, region4, entry5, state11, exit14, transition13.
declare transition14, finalState6, state12, finalState7, state13, exit15.
Transition(transition14).
Statechart(sct).
State(state1).
Region(region1).
Choice(choice1).
FinalState(finalState1).
Region(region2).
Choice(exit1).
State(state2).
Exit(state3).
FinalState(finalState2).
State(exit2).
Choice(exit3).
Transition(transition2).
Region(exit4).
Exit(exit5).
Transition(transition3).
Transition(transition4).
Transition(transition5).
Entry(entry2).
FinalState(finalState3).
Transition(transition6).
Exit(exit6).
Exit(exit7).
State(state4).
State(state5).
Entry(entry3).
State(state6).
Exit(exit8).
Transition(transition9).
FinalState(finalState4).
Exit(exit9).
Region(region3).
State(state7).
Exit(exit10).
Transition(transition10).
Exit(exit11).
Exit(exit12).
Transition(transition11).
State(state8).
State(state9).
FinalState(finalState5).
Exit(exit13).
State(state10).
Transition(transition12).
Region(region4).
State(state11).
Exit(exit14).
Transition(transition13).
FinalState(finalState6).
State(state12).
FinalState(finalState7).
State(state13).
regions(sct, region1).
regions(sct, exit4).
regions(state2, region3).
regions(state4, region4).
regions(state9, region2).
vertices(region1, choice1).
vertices(region1, finalState1).
vertices(region1, exit2).
vertices(region1, entry2).
vertices(region1, finalState3).
vertices(region1, exit6).
vertices(region1, state6).
vertices(region1, exit10).
vertices(region1, state8).
vertices(region2, state1).
vertices(region2, exit1).
vertices(region2, state2).
vertices(region2, state3).
vertices(region2, entry1).
vertices(region2, exit7).
vertices(region2, state5).
vertices(region2, finalState5).
vertices(region2, exit13).
vertices(region2, state12).
vertices(region2, finalState7).
vertices(exit4, exit5).
vertices(exit4, entry3).
vertices(exit4, exit8).
vertices(exit4, finalState4).
vertices(exit4, exit9).
vertices(exit4, exit12).
vertices(exit4, state10).
vertices(exit4, exit14).
vertices(region3, finalState2).
vertices(region3, state7).
vertices(region3, exit11).
vertices(region3, entry4).
vertices(region3, finalState6).
vertices(region3, exit15).
vertices(region4, exit3).
vertices(region4, entry5).
vertices(region4, state13).
outgoingTransition(choice1, transition14).
outgoingTransition(choice1, transition1).
outgoingTransition(exit1, transition4).
outgoingTransition(exit1, transition8).
outgoingTransition(state2, transition2).
outgoingTransition(state2, transition5).
outgoingTransition(entry2, transition6).
outgoingTransition(entry3, transition11).
outgoingTransition(state6, transition7).
outgoingTransition(entry4, transition9).
outgoingTransition(state8, transition3).
outgoingTransition(entry5, transition13).
incomingTransition(choice1, transition7).
incomingTransition(exit1, transition14).
incomingTransition(state2, transition6).
incomingTransition(state3, transition12).
incomingTransition(finalState2, transition10).
incomingTransition(exit2, transition11).
incomingTransition(exit3, transition4).
incomingTransition(exit5, transition8).
incomingTransition(finalState3, transition5).
incomingTransition(state4, transition13).
incomingTransition(state6, transition3).
incomingTransition(state9, transition1).
